export const CHAPTERS = [
  {
    id: "basics", title: "Shell Basics", icon: "💻", color: "#f9c74f", difficulty: "beginner",
    desc: "Your first commands, navigation, and getting help",
    badge: "Explorer",
    teach: [
      "Welcome to the command line. The terminal (or terminal emulator) is a text-based interface to your computer — instead of clicking icons, you type commands. It's how developers, sysadmins, and data scientists interact with servers, automate workflows, and get things done fast.",
      "Every command follows the pattern `command [flags] [arguments]`. The command is what to do, flags modify how, and arguments say what to act on.",
      "The filesystem is a tree of directories (folders) and files. You're always 'inside' one directory. `pwd` shows where you are, `ls` shows what's here, `cd` moves you somewhere else.",
      "Press `Tab` for a hint if you're stuck, `↑`/`↓` to recall previous commands. Open a real terminal alongside this tutorial and try each command there too — you'll learn faster by comparing.",
    ],
    steps: [
      { learn: "`echo` prints text to the terminal. It's the simplest command — type `echo` followed by any text, and it repeats it back. A good first command to verify everything works.", task: "Use `echo` to print the word 'hello'", hint: "echo hello", accept: ["echo hello", "echo 'hello'", 'echo "hello"'], output: "hello", note: "`echo` is used everywhere in scripts: printing status messages, displaying variable values, and writing to files." },
      { learn: "When text contains spaces, quotes group it as one argument. Single quotes are literal, double quotes allow variable expansion (like `$HOME`). Without quotes, the shell treats each word as a separate argument — it still works with `echo`, but matters for other commands.", task: "Use `echo` with quotes to print 'hello world'", hint: 'echo "hello world"', accept: ["echo hello world", 'echo "hello world"', "echo 'hello world'"], output: "hello world", note: "Single quotes: everything is literal. Double quotes: variables like `$HOME` get expanded. No quotes: spaces split into separate arguments. This matters for filenames with spaces, passing arguments, and scripting." },
      { learn: "`whoami` prints the username of the current user. No arguments needed. On a real server, this tells you which account you're logged in as.", task: "Print your current username with `whoami`", hint: "whoami", accept: ["whoami"], output: "ubuntu", note: "You're 'ubuntu'. On your own machine it'll be your username. This matters when checking file permissions or SSH access — you need to know who you are to understand what you can do." },
      { learn: "`hostname` prints the name of the machine you're on. Useful when you're SSH'd into a remote server and need to confirm which box you're working on.", task: "Print the name of this machine with `hostname`", hint: "hostname", accept: ["hostname"], output: "gpu-box", note: "This machine is called 'gpu-box'. When you're working across multiple servers via SSH, `hostname` keeps you oriented — especially important so you don't accidentally run commands on the wrong machine." },
      { learn: "`pwd` (print working directory) shows your current location in the filesystem. Think of it as GPS — it tells you exactly which folder you're 'inside' right now.", task: "Print your current directory with `pwd`", hint: "pwd", accept: ["pwd"], output: "/home/ubuntu", note: "You're in `/home/ubuntu` — your home directory. Every user has one. The `~` shortcut always refers to your home directory." },
      { learn: "`ls` lists the contents of the current directory. By itself, it shows file and folder names. This is one of the most-used commands — you'll run it constantly.", task: "Run `ls` to see what's in the current directory", hint: "ls", accept: ["ls"], output: "project  notes.md  scripts", note: "Three items visible. But there are also hidden files (starting with `.`) that `ls` doesn't show by default — you'll reveal them next with `ls -a`." },
      { learn: "`ls -l` shows the 'long' format: permissions, owner, size, and modification date for each file. The first character shows `d` for directory, `-` for file.", task: "Run `ls -l` to see file details", hint: "ls -l", accept: ["ls -l"], output: "drwxr-xr-x  5 ubuntu ubuntu 4096 Mar 18 22:15 project\n-rw-r--r--  1 ubuntu ubuntu  120 Mar 15 11:20 notes.md\ndrwxr-xr-x  3 ubuntu ubuntu 4096 Mar 12 14:00 scripts", note: "The `rwx` columns show read/write/execute permissions. You'll learn more about these in the Permissions chapter." },
      { learn: "Files starting with `.` are hidden by default (like `.bashrc`, `.ssh`). `ls -a` shows everything, including hidden files. The `.` entry is the current directory, `..` is the parent.", task: "Run `ls -a` to see hidden files too", hint: "ls -a", accept: ["ls -a"], output: ".  ..  .bashrc  .ssh  project  notes.md  scripts", note: "`.bashrc` is your shell config (runs on every new terminal). `.ssh` holds your SSH keys. Hidden files are usually configs — they're hidden so they don't clutter `ls` output." },
      { learn: "You can combine flags: `ls -la` shows all files (including hidden) in long format. This is the 'show me everything' command. You can also write it as `ls -al` or `ls -l -a` — the order doesn't matter.", task: "Run `ls -la` to see all files with full details", hint: "ls -la", accept: ["ls -la", "ls -al", "ls -lah"], output: "drwxr-xr-x  5 ubuntu ubuntu 4096 Mar 18 22:15 .\ndrwxr-xr-x  3 ubuntu ubuntu 4096 Mar 18 22:00 ..\n-rw-r--r--  1 ubuntu ubuntu 3771 Mar  5 14:00 .bashrc\ndrwxr-xr-x  2 ubuntu ubuntu 4096 Mar 10 09:30 .ssh\ndrwxr-xr-x  8 ubuntu ubuntu 4096 Mar 17 16:45 project\n-rw-r--r--  1 ubuntu ubuntu  120 Mar 15 11:20 notes.md\ndrwxr-xr-x  3 ubuntu ubuntu 4096 Mar 12 14:00 scripts", note: "This is probably the command you'll use most. `ls -lah` adds human-readable sizes (e.g. '4.0K' instead of '4096'). Try running `ls -la` in your own terminal to see your real files." },
      { learn: "`man` (manual) opens the built-in documentation for any command. It's the most detailed reference available — right in your terminal. Navigate with arrow keys or `j`/`k`, search with `/`, and press `q` to quit. Whenever you're unsure about a command or its flags, `man` should be your first stop.", task: "Open the manual page for `ls`", hint: "man ls", accept: ["man ls"], output: "LS(1)\n\nNAME\n       ls - list directory contents\n\nSYNOPSIS\n       ls [OPTION]... [FILE]...\n\nDESCRIPTION\n       -a    do not ignore entries starting with .\n       -l    use a long listing format\n       -h    human-readable sizes\n       -R    list subdirectories recursively\n       -S    sort by file size\n       -t    sort by modification time\n\n  / to search · q to quit", note: "Try `man` for any command: `man grep`, `man chmod`, `man bash`. Before searching the web, try `man [command]` — it's faster and always up to date for your system. Make it a habit." },
      { learn: "Many commands also support `--help` as a quick alternative to `man`. It prints a shorter summary of usage and flags directly in the terminal. Not every command supports it, but most do. It's handy when you just need a quick reminder of the syntax.", task: "Run `ls --help` to see a quick summary", hint: "ls --help", accept: ["ls --help"], output: "Usage: ls [OPTION]... [FILE]...\nList directory contents.\n\n  -a            do not ignore entries starting with .\n  -l            use a long listing format\n  -h            human-readable sizes\n  -R            list subdirectories recursively\n  -S            sort by file size, largest first\n  -t            sort by time, newest first\n  --color       colorize the output", note: "`man` gives full documentation, `--help` gives a quick cheat sheet. Use `--help` when you need a fast reminder, `man` when you need the full picture. Some commands use `-h` instead of `--help` — if one doesn't work, try the other. Note: on macOS, `ls --help` doesn't work because macOS uses BSD coreutils, not GNU. Use `man ls` instead." },
      { learn: "`cd` (change directory) moves you into a folder. Just type `cd` followed by the directory name.", task: "Use `cd` to move into the `project` directory", hint: "cd project", accept: ["cd project", "cd project/", "cd ./project"], output: "", note: "Now you're in `/home/ubuntu/project`. Use `pwd` to confirm. `cd` is how you navigate the filesystem." },
      { learn: "`cd ..` goes up one directory level. `..` always means 'parent directory'. `cd ../..` goes up two levels. `cd ~` goes straight home. `cd -` goes back to where you just were.", task: "Use `cd ..` to go back up one directory", hint: "cd ..", accept: ["cd ..", "cd ../"], output: "", note: "Back in `/home/ubuntu`. The pattern: `cd` into something, do your work, `cd ..` to come back." },
      { learn: "`clear` resets the terminal screen. It doesn't delete anything — just scrolls the clutter away. Keyboard shortcuts: `Ctrl+L` on Linux, `Cmd+K` on macOS (clears scrollback too).", task: "Type `clear` to clean up the terminal", hint: "clear", accept: ["clear"], output: "", note: "Use `clear` or `Ctrl+L` whenever your terminal gets cluttered. `Ctrl+L` is faster since your hands stay on the keyboard." },
      { learn: "`history` shows all the commands you've typed in this session. On a real terminal, it persists across sessions in `~/.bash_history`. Press `↑` to recall the previous command, or use `Ctrl+R` to search your history.", task: "Type `history` to see your command history", hint: "history", accept: ["history"], output: "1  echo hello\n2  whoami\n3  hostname\n4  pwd\n5  ls\n6  cd project", note: "Your command history is a productivity tool. `Ctrl+R` opens reverse search — start typing and it finds matching commands. On a real terminal, `!42` re-runs command #42." },
    ],
  },
  {
    id: "files", title: "Files & Directories", icon: "📁", color: "#f9c74f", difficulty: "beginner",
    desc: "Create, copy, move, delete, and find files",
    badge: "Organizer",
    teach: [
      "Now that you can navigate the filesystem, it's time to manage it. This chapter covers creating, copying, moving, and deleting files and directories — the core operations you'll use every day.",
      "You'll also learn `find` to search for files, `du` to check disk usage, and `sudo` to run commands with elevated privileges.",
      "A key safety rule: `rm` deletes permanently. There is no trash can. Always double-check before deleting.",
    ],
    steps: [
      { learn: "`mkdir` creates a new directory. Syntax: `mkdir [name]`. Use `-p` to create nested paths: `mkdir -p data/raw/2026` creates all three levels at once, even if `data/` doesn't exist yet.", task: "Create a new directory called `logs`", hint: "mkdir logs", accept: ["mkdir logs", "mkdir -p logs"], output: "", note: "Created. Without `-p`, `mkdir a/b/c` fails if `a/b` doesn't exist. With `-p`, it creates the whole path." },
      { learn: "Let's move into the directory we just created. Building habits: create a directory, then `cd` into it to work.", task: "Move into the `logs` directory you just created", hint: "cd logs", accept: ["cd logs", "cd logs/"], output: "", note: "You're in `/home/ubuntu/logs`. An empty directory, ready for files." },
      { learn: "`touch` creates an empty file, or updates the timestamp of an existing file. It's the quickest way to create a new file. Syntax: `touch [filename]`.", task: "Create an empty file called `notes.txt`", hint: "touch notes.txt", accept: ["touch notes.txt"], output: "", note: "`notes.txt` now exists (0 bytes). `touch` is also useful for creating placeholder files or updating timestamps for build systems." },
      { learn: "Let's go back to the home directory. In real work, you'll bounce between directories constantly.", task: "Go back up to the home directory", hint: "cd ..", accept: ["cd ..", "cd ../", "cd ~"], output: "", note: "Back home. The create-enter-work-leave cycle is a core terminal workflow." },
      { learn: "`cp` copies files. Syntax: `cp [source] [destination]`. The original stays untouched. Add `-r` to copy entire directories.", task: "Copy `config.yaml` to `config.backup.yaml`", hint: "cp config.yaml config.backup.yaml", accept: ["cp config.yaml config.backup.yaml"], output: "", note: "Always back up configs before editing them. `cp -r src/ backup/` copies an entire directory. Unlike `mv`, the original remains." },
      { learn: "`mv` both moves AND renames files. If the destination is a directory, the file moves into it. If it's a filename, the file gets renamed. There's no separate 'rename' command.", task: "Rename `notes.md` to `README.md`", hint: "mv notes.md README.md", accept: ["mv notes.md README.md"], output: "", note: "`mv file.txt /tmp/` moves it. `mv old.txt new.txt` renames it. Unlike `cp`, the original is gone after `mv`." },
      { learn: "`rm` deletes files permanently — there is no trash can, no undo. Unlike your desktop where deleted files go to a Recycle Bin or Trash, `rm` erases the file immediately and it cannot be recovered.", task: "Delete the file `old.log`", hint: "rm old.log", accept: ["rm old.log"], output: "", note: "Gone. No confirmation, no undo. Always double-check the filename before pressing Enter. In your own terminal, try `rm -i file` which asks for confirmation before deleting — a safer habit." },
      { learn: "`rm -r` removes directories and everything inside them recursively. `rm -rf` adds the force flag, skipping all confirmation prompts. This is the most dangerous command in Linux. Running `rm -rf /` or `rm -rf ~` will destroy your entire system or home directory. Never run `rm -rf` unless you are 100% sure of the path. There is no undo.", task: "Remove the empty directory `tmp_logs/` using `rm -r tmp_logs/`", hint: "rm -r tmp_logs/", accept: ["rm -r tmp_logs/", "rm -r tmp_logs", "rm -r ./tmp_logs/", "rm -r ./tmp_logs"], output: "", note: "NEVER run `rm -rf /` or `rm -rf ~` — this will permanently destroy your system or all your personal files. Real sysadmins have lost entire servers this way. Safer alternatives: use `rm -ri` (asks before each file), or `trash-cli` which moves files to trash instead of deleting them. When in doubt, `ls` the directory first to see what's inside before removing it." },
      { learn: "`find` searches for files by name, size, type, or modification time recursively through all subdirectories. Typing `.` searches the current directory. To match all .py files we can type `\"*.py\"` Syntax: `find [where] -name [pattern]`.", task: "Use `find` starting from `.` to locate all `.py` files", hint: 'find . -name "*.py"', accept: ['find . -name "*.py"', "find . -name '*.py'", "find . -name *.py"], output: "./src/train.py\n./src/inference.py\n./src/utils.py\n./tests/test_model.py", note: "The `.` means 'start from here'. `*` is a wildcard matching anything. Try this in your own terminal — `find` is much more powerful with real files. Run `man find` to see the full list of options." },
      { learn: "`du` (disk usage) shows how much space files and directories use. `-s` gives a summary per item, `-h` makes sizes human-readable (KB, MB, GB instead of raw bytes).", task: "Check disk usage of each item with `du -sh *`", hint: "du -sh *", accept: ["du -sh *", "du -sh ./*"], output: "12K     config\n2.4G    data\n8.7G    models\n24K     src\n4.0K    Dockerfile", note: "Models: 8.7GB, data: 2.4GB. When your disk fills up, `du -sh * | sort -rh` sorts by size to find the biggest culprits." },
      { learn: "`sudo` runs a command as the root (admin) user. Some operations need elevated privileges — installing software, editing system files, restarting services. You'll be asked for your password the first time.", task: "Run `sudo whoami` to execute as root", hint: "sudo whoami", accept: ["sudo whoami"], output: "root", note: "Without `sudo`: 'Permission denied'. With `sudo`: runs as root. Use carefully — root can break things. Only use `sudo` when you actually need elevated access." },
    ],
  },
  {
    id: "perm", title: "Permissions & Env", icon: "🔐", color: "#f9c74f", difficulty: "beginner",
    desc: "File permissions, ownership, and environment variables",
    badge: "Gatekeeper",
    teach: [
      "Linux is a multi-user system. Permissions control exactly who can read, write, and execute every file. Getting this right is critical for security — a misconfigured permission can expose secrets or let attackers in.",
      "Every file has three permission groups: owner (the user who created it), group (a set of users), and others (everyone else). Each group gets read (`r`), write (`w`), and/or execute (`x`) access.",
      "Numeric notation makes this compact: `7`=rwx (full), `5`=r-x (read+execute), `4`=r-- (read only), `0`=--- (none). So `chmod 755` means owner=rwx, group=r-x, others=r-x.",
      "You'll also learn `chown` (change ownership), environment variables, and how to run shell scripts — files containing a sequence of commands that the shell executes top to bottom.",
    ],
    steps: [
      { learn: "File permissions have three groups: the owner (the user who created it), the group (users in the same group), and others (everyone else). Each group can have `r` (read), `w` (write), and `x` (execute). `ls -l` shows these as 9 characters: `rwxr-xr-x` = owner:rwx, group:r-x, others:r-x.", task: "Run `ls -l deploy.sh` to check its permissions", hint: "ls -l deploy.sh", accept: ["ls -l deploy.sh", "ls -la deploy.sh"], output: "-rw-r--r--  1 ubuntu ubuntu 342 Mar 15 11:20 deploy.sh", note: "`rw-r--r--` means owner can read/write, everyone else can only read. No `x` means it's not executable — if you tried to run it right now with `./deploy.sh`, you'd get 'Permission denied'." },
      { learn: "A shell script is a text file containing a list of commands that the shell executes line by line. The first line should be `#!/bin/bash` (called a 'shebang') — it tells the system which interpreter to use. Let's look at what's inside this script before we try to run it.", task: "Run `cat deploy.sh` to see what's inside the script", hint: "cat deploy.sh", accept: ["cat deploy.sh"], output: "#!/bin/bash\necho \"Starting deploy...\"\ngit pull origin main\nnpm install\nnpm run build\necho \"Deploy complete!\"", note: "Each line is a command that runs in order, top to bottom — exactly as if you typed them one by one. The `#!/bin/bash` shebang on line 1 tells the OS to run this file with bash. You can put any commands in a script that you'd type in the terminal." },
      { learn: "To run a script, you use `./` followed by the filename. The `./` tells the shell to look in the current directory instead of searching `$PATH`. Let's try running `deploy.sh` now.", task: "Try running the script with `./deploy.sh`", hint: "./deploy.sh", accept: ["./deploy.sh"], output: "bash: ./deploy.sh: Permission denied", note: "Permission denied! Remember, `ls -l` showed no `x` in the permissions — the file isn't executable. The commands inside are fine, but the OS won't let you run the file. We need to fix the permissions first." },
      { learn: "`chmod +x` makes a file executable. We just saw 'Permission denied' because `deploy.sh` has no execute permission. Adding `+x` is what allows you to run a script.", task: "Use `chmod +x` to make `deploy.sh` executable", hint: "chmod +x deploy.sh", accept: ["chmod +x deploy.sh"], output: "", note: "Now the file can be executed. This is the step most beginners forget — you write a script, try to run it, and get 'Permission denied'. The fix is always `chmod +x`." },
      { learn: "Now that we've added execute permission, let's try running the script again. Same command as before — but this time it will work.", task: "Run the script with `./deploy.sh`", hint: "./deploy.sh", accept: ["./deploy.sh", "bash deploy.sh"], output: "Starting deploy...\nAlready up to date.\nadded 0 packages\n✓ Build complete\nDeploy complete!", note: "It works now! The only difference is the permission. You can also run any script with `bash deploy.sh` — that bypasses the execute check because you're telling bash directly to interpret the file." },
      { learn: "The `.sh` extension is just a convention — bash doesn't require it. A script named `backup` works the same as `backup.sh` as long as it has the shebang (`#!/bin/bash`) and execute permission. The extension is for humans, not the computer. This tutorial focuses on using the command line, not writing scripts — for bash scripting, online resources are a great next step.", task: "Run `./backup` to execute a script without the `.sh` extension", hint: "./backup", accept: ["./backup", "bash backup"], output: "Backing up /home/ubuntu/project...\ntar -czf backup-2026-03-21.tar.gz project/\nBackup complete: backup-2026-03-21.tar.gz (2.4MB)", note: "It runs the same way. Many CLI tools you install (like `docker`, `kubectl`, `terraform`) are actually scripts or binaries sitting in a `$PATH` directory with no extension. Convention: use `.sh` for your own scripts so they're easy to identify, but know it's optional." },
      { learn: "`chmod` with numbers sets exact permissions. `755` = rwxr-xr-x, `644` = rw-r--r--, `600` = rw-------.", task: "Run `chmod 755 deploy.sh` to set standard script permissions", hint: "chmod 755 deploy.sh", accept: ["chmod 755 deploy.sh"], output: "", note: "`755` is the standard for scripts and executables. Owner has full control, everyone else can read and execute." },
      { learn: "`chmod 600` means only the owner can read/write. Essential for SSH keys and credential files.", task: "Run `chmod 600 .env.secrets` to lock down the secrets file", hint: "chmod 600 .env.secrets", accept: ["chmod 600 .env.secrets"], output: "", note: "SSH refuses to use keys with loose permissions. Any file with API keys or passwords should be `600`." },
      { learn: "`chown` (change owner) sets who owns a file. `-R` applies to all files in a directory. You need `sudo` because only root can change file ownership — otherwise any user could claim ownership of any file, which would be a security disaster.", task: "Run `sudo chown -R ubuntu:ubuntu /data/` to take ownership of `/data/`", hint: "sudo chown -R ubuntu:ubuntu /data/", accept: ["sudo chown -R ubuntu:ubuntu /data/", "chown -R ubuntu:ubuntu /data/"], output: "", note: "Docker often creates files owned by root. `chown` fixes it so your app can read/write." },
      { learn: "`export` sets an environment variable. Programs you launch afterward can read it.", task: "Use `export` to set `CUDA_VISIBLE_DEVICES` to `0`", hint: "export CUDA_VISIBLE_DEVICES=0", accept: ["export CUDA_VISIBLE_DEVICES=0"], output: "", note: "Any Python script now only sees GPU 0. For one command: `CUDA_VISIBLE_DEVICES=1 python train.py` (no `export` needed)." },
      { learn: "`$HOME` is an environment variable that stores the path to your home directory. The `$` prefix tells the shell to expand the variable name into its value — without `$`, `echo HOME` just prints the word 'HOME'. Environment variables are built-in settings that your shell and programs use to know things about your system — where your home folder is, which user you are, and where to find commands.", task: "Run `echo $HOME` to print your home directory path", hint: "echo $HOME", accept: ["echo $HOME"], output: "/home/ubuntu", note: "Common built-in variables: `$HOME` (home directory), `$USER` (your username), `$SHELL` (which shell you're using), `$PATH` (where commands are found). You can also create your own with `export`. Variables are uppercase by convention." },
      { learn: "`source` reads a file and executes every line in your current shell session, as if you typed each line yourself. A `.env` file typically contains `export` lines that set environment variables (like API keys or config values). Running `source .env` loads all of those variables into your current terminal so that any program you run afterward can access them. Without `source`, the variables stay trapped inside the file.", task: "Run `source .env` to load environment variables into your shell", hint: "source .env", accept: ["source .env", ". .env"], output: "", note: "`. .env` is shorthand for `source .env` — they do the same thing. You'll also use `source` to activate Python virtual environments: `source venv/bin/activate`. The key idea: `source` runs commands in your current shell, while `./script.sh` runs them in a new sub-shell (so variables set inside don't carry over)." },
      { learn: "`$PATH` is one of the most important environment variables. It contains a list of directories, separated by `:`, where the shell looks for commands. When you type `python`, `git`, or even `ls`, the shell searches each directory in `$PATH` left to right until it finds a matching executable. That's why you can type `ls` instead of `/usr/bin/ls` — the shell finds it for you.", task: "Run `echo $PATH` to see your command search path", hint: "echo $PATH", accept: ["echo $PATH"], output: "/usr/local/sbin:/usr/local/bin:/usr/bin:/usr/sbin:/home/ubuntu/.local/bin", note: "Each `:` separates a directory. The shell checks them left to right. If you put a script in any of these directories (like `/usr/local/bin/`), you can run it by name from anywhere — no `./` needed. That's how installed tools work." },
      { learn: "`which` tells you exactly which executable the shell will run when you type a command. It searches `$PATH` (which you just saw) and shows the full file path. This is essential for debugging — if `python` is the wrong version, `which python` shows you which binary is being used, so you can figure out why.", task: "Run `which python` to find where Python is installed", hint: "which python", accept: ["which python", "which python3"], output: "/usr/bin/python3", note: "The shell found `python3` in `/usr/bin/` — one of the directories in your `$PATH`. If you had multiple Python versions installed, `which` tells you which one wins. Try `which git`, `which bash`, etc. to see where your commands live." },
    ],
  },
  {
    id: "custom", title: "Customise Your Shell", icon: "🎨", color: "#f9c74f", difficulty: "beginner",
    desc: "Themes, fonts, aliases, prompts, and colors — make it yours",
    badge: "Customiser",
    teach: [
      "Your terminal has two layers: the emulator (the window app) and the shell (the program inside it). You will customise both in this chapter.",
      "The emulator controls how things look: themes, fonts, colors. The shell controls how things work: commands, aliases, prompts, environment variables.",
      "Everything you set up here will persist across all future lessons.",
    ],
    steps: [
      // Part 1: Terminal Settings (emulator + prompt)
      { type: "theme-picker", learn: "Let's set up your terminal. The theme, font, and text size are emulator settings — in real life you change these in your terminal app's preferences. The prompt (PS1) is a shell setting. Use the controls on the left to customise everything, then type `done`.", task: "Pick a theme, font, text size, and prompt using the controls on the left, then type `done`", hint: "done", accept: ["done"], output: "Terminal settings applied.", note: "You just configured your terminal emulator and your shell prompt. In real life: iTerm2 has Profiles > Colors, Windows Terminal has settings.json, Alacritty uses a TOML config. The prompt is set with the PS1 variable in your shell config file. Popular frameworks like oh-my-zsh, Starship, and Powerlevel10k all work by setting PS1 and adding functions to your startup file." },

      // Part 2: The Shell (the program inside)
      { learn: "Now let's look at the inner layer: the shell. The shell is the program interpreting your commands. `echo $SHELL` shows which one you are running.", task: "Run `echo $SHELL` to see your current shell", hint: "echo $SHELL", accept: ["echo $shell"], output: "/bin/bash", note: "The shell is the program running inside your terminal emulator. It interprets your commands. Common shells: bash (default on most Linux), zsh (default on macOS since Catalina), fish (friendlier defaults, different syntax). To switch permanently: `chsh -s /bin/zsh`. To try temporarily, just type the shell name. Each shell has its own config file and slightly different syntax." },
      { learn: "Your shell has its own config file called `.bashrc` (or `.zshrc` for zsh). This startup script runs every time you open a terminal. Let's see what is in it.", task: "Run `cat ~/.bashrc` to see your shell config", hint: "cat ~/.bashrc", accept: ["cat ~/.bashrc", "cat .bashrc"], output: "__cat_bashrc__", note: "This is your shell's startup script. Every time you open a terminal, the shell reads this file and runs every line. Aliases, prompt settings, and environment variables go here. The emulator has its own separate config. Two layers, two config files." },

      // Part 3: Shell Commands (customising your shell)
      { learn: "An alias creates a shortcut. `alias please=\"sudo\"` means every time you type `please`, the shell replaces it with `sudo` before executing. You are literally inventing new commands.", task: "Create an alias: `alias please=\"sudo\"`", hint: "alias please=\"sudo\"", accept: ["alias please=\"sudo\"", "alias please='sudo'", "alias please=sudo"], output: "", note: "You just invented a command. Aliases are shell shortcuts stored in memory. They vanish when you close the terminal unless you save them to your startup file. This is a shell feature, not an emulator feature." },
      { learn: "Now use your alias. `please cat /etc/shadow` expands to `sudo cat /etc/shadow`. The file `/etc/shadow` stores password hashes and normally requires root access.", task: "Use your alias: `please cat /etc/shadow`", hint: "please cat /etc/shadow", accept: ["please cat /etc/shadow"], output: "root:$6$xyz...:19000:0:99999:7:::\ndaemon:*:19000:0:99999:7:::\nbin:*:19000:0:99999:7:::", note: "/etc/shadow stores password hashes and normally requires root access. Your alias expanded `please` to `sudo`, which ran the command as root." },
      { learn: "`LS_COLORS` tells `ls` how to color different file types. The format uses codes: `di` means directory, `1;36` means bold cyan. On macOS, BSD `ls` uses a different variable called `LSCOLORS` with its own syntax. Modern alternatives like `eza` have their own color config.", task: "Set file colors: `export LS_COLORS=\"di=1;36:*.py=1;33:*.md=1;32:*.sh=1;31\"`", hint: "export LS_COLORS=\"di=1;36:*.py=1;33:*.md=1;32:*.sh=1;31\"", accept: ["export ls_colors=\"di=1;36:*.py=1;33:*.md=1;32:*.sh=1;31\"", "export ls_colors='di=1;36:*.py=1;33:*.md=1;32:*.sh=1;31'"], output: "", note: "LS_COLORS tells ls how to color different file types. This is the GNU/Linux syntax. macOS uses a different variable (LSCOLORS) with different codes. Modern alternatives like eza have their own color config." },
      { learn: "Now run `ls` to see your colors in action. Each file type gets its own color based on the LS_COLORS you just set.", task: "Run `ls` to see colorized output", hint: "ls", accept: ["ls"], output: "__colored_ls__", note: "Directories are cyan, Python files yellow, Markdown green, shell scripts red. These colors are a shell feature, controlled by an environment variable. The emulator just renders whatever colors the shell tells it to." },

      // Part 4: The Vanishing Trick (why config files matter)
      { learn: "Imagine you just closed your terminal and opened a brand new one. A fresh session. All shell memory is wiped clean. Your alias, your custom prompt, your LS_COLORS — all gone. Try running your alias now.", task: "A new session just started — try `please cat /etc/shadow`", hint: "please cat /etc/shadow", accept: ["please cat /etc/shadow"], output: "__vanish_fail__", note: "A new session just started. Aliases live in memory and memory is wiped when the terminal closes. Your theme and font are fine because those are emulator settings, saved in the emulator's config. But aliases and your custom prompt were shell settings that were never saved to a file." },
      { learn: "The `>>` operator appends a line to the end of a file without overwriting it. Let's save your alias to `.bashrc` so it loads automatically next time.", task: "Save your alias: `echo 'alias please=\"sudo\"' >> ~/.bashrc`", hint: "echo 'alias please=\"sudo\"' >> ~/.bashrc", accept: ["echo 'alias please=\"sudo\"' >> ~/.bashrc", "echo \"alias please=\\\"sudo\\\"\" >> ~/.bashrc"], output: "", note: "The >> operator appends to a file without overwriting it. Your alias is now saved in .bashrc. Startup file order: .bash_profile loads for login shells, .bashrc loads for interactive shells. On macOS, Terminal opens login shells so use .bash_profile or .zshrc. Most people add `source ~/.bashrc` inside .bash_profile so both files work." },
      { learn: "`source` reruns your config file in the current shell without restarting. This is the pattern you will repeat: edit the file, source it, test.", task: "Reload your config: `source ~/.bashrc`", hint: "source ~/.bashrc", accept: ["source ~/.bashrc", ". ~/.bashrc"], output: "__source_bashrc__", note: "`source` means 'run this file right now in the current shell.' Every plugin manager, version manager, and framework works this way: they add lines to your startup file, then source it to apply changes without restarting." },
      { learn: "The moment of truth. Your alias was saved to `.bashrc` and reloaded with `source`. It should work again.", task: "Test your alias: `please cat /etc/shadow`", hint: "please cat /etc/shadow", accept: ["please cat /etc/shadow"], output: "root:$6$xyz...:19000:0:99999:7:::\ndaemon:*:19000:0:99999:7:::\nbin:*:19000:0:99999:7:::", note: "Your alias is back because source reloaded .bashrc. This is the cycle you will repeat whenever you change your shell config: edit the file, source it, test. Your emulator settings (theme, font) survived the restart because they are stored separately by the terminal app." },
    ],
  },
  {
    id: "tools", title: "Links, Archives & Editors", icon: "🔧", color: "#f9c74f", difficulty: "beginner",
    desc: "Symlinks, aliases, compression, and vim",
    badge: "Toolsmith",
    teach: [
      "This chapter covers power tools: symlinks (filesystem shortcuts), aliases (command shortcuts), compression (tar & zip), vim (the terminal text editor), and downloading/transferring data with curl and wget.",
      "These tools come up constantly in real work — deploying code, packaging projects, editing configs on remote servers, downloading data, testing APIs, and creating shortcuts that save you time every day.",
    ],
    steps: [
      { learn: "A symlink (symbolic link) is a special type of file that acts as a pointer or shortcut to another file or directory. It contains a path to the target file or folder, allowing access to it from a different location in the file system without duplicating the actual data. When you open the symlink, the system follows the pointer and opens the real file. Syntax: `ln -s [target] [link_name]` (`ln` = link, `-s` = symbolic).", task: "Create a symlink called `current` pointing to `models/v2`", hint: "ln -s models/v2 current", accept: ["ln -s models/v2 current"], output: "", note: "Now `current` points to `models/v2`. Your code can always read from `current/` and you switch model versions by changing where the link points — no code changes needed." },
      { learn: "Use `ls -l` to verify your symlink. Symlinks show up with an arrow (`->`) pointing to their target. The first character in the permissions is `l` for link.", task: "Run `ls -l current` to see where the symlink points", hint: "ls -l current", accept: ["ls -l current"], output: "lrwxrwxrwx 1 ubuntu ubuntu 9 Mar 21 10:00 current -> models/v2", note: "See the `l` at the start and the `->` arrow? That's how you spot a symlink. To update it: `ln -sf models/v3 current` — the `-f` flag forces overwriting the old link. One command, instant version switch." },
      { learn: "Symlink vs alias — know the difference. A symlink is a filesystem shortcut: it creates an actual file on disk that any program (Python, Node, nginx, etc.) can follow. An `alias` is a shell shortcut: it only works when you type commands in the terminal. Use symlinks when programs need to find files (e.g. `current -> models/v2`). Use aliases when you want to save typing (e.g. `alias gs='git status'`).", task: "Create an alias `ll` for `ls -la`", hint: "alias ll='ls -la'", accept: ["alias ll='ls -la'", "alias ll=\"ls -la\""], output: "", note: "Alias created — but only for this terminal session. Aliases vanish when you close the terminal. To make them permanent, add the alias line to your `~/.bashrc` or `~/.zshrc` file. Symlinks, by contrast, persist on disk permanently until you delete them." },
      { learn: "Now test the alias you just created. Type `ll` and it will expand to `ls -la` automatically. This is the power of aliases — turn long commands into short ones you can type from muscle memory.", task: "Type `ll` to test your new alias", hint: "ll", accept: ["ll"], output: "total 48\ndrwxr-xr-x  8 ubuntu ubuntu 4096 Mar 21 10:00 .\ndrwxr-xr-x  3 ubuntu ubuntu 4096 Mar 21 09:00 ..\n-rw-r--r--  1 ubuntu ubuntu  512 Mar 21 10:00 config.yaml\nlrwxrwxrwx  1 ubuntu ubuntu    9 Mar 21 10:00 current -> models/v2\ndrwxr-xr-x  2 ubuntu ubuntu 4096 Mar 21 10:00 data\ndrwxr-xr-x  3 ubuntu ubuntu 4096 Mar 21 10:00 models\ndrwxr-xr-x  2 ubuntu ubuntu 4096 Mar 21 10:00 src", note: "It ran `ls -la` for you. Notice the symlink `current -> models/v2` in the listing too. Common aliases: `alias gs='git status'`, `alias k='kubectl'`, `alias dc='docker compose'`. Build your own collection over time." },
      { learn: "`tar` bundles files into a single archive. The flags: `c` = create, `z` = compress with gzip, `f` = filename follows. So `tar -czf` means 'create a gzipped archive to this file'. The result is a `.tar.gz` file. The format is `tar -czf [archive_name] [source]`.", task: "Compress the `src/` directory into `src.tar.gz`", hint: "tar -czf src.tar.gz src/", accept: ["tar -czf src.tar.gz src/", "tar czf src.tar.gz src/", "tar -czf src.tar.gz src"], output: "", note: "`.tar.gz` = tarball. It's the standard archive format on Linux. `tar` alone bundles files (no compression), `gz` adds compression. Memory aid: Create Zipped File = `czf`." },
      { learn: "`tar -xzf` extracts a compressed archive. `x` = extract, `z` = gzip, `f` = file. You can add `-C /path/` to extract to a specific directory.", task: "Extract `src.tar.gz`", hint: "tar -xzf src.tar.gz", accept: ["tar -xzf src.tar.gz", "tar xzf src.tar.gz"], output: "", note: "Extracted to `./src/`. Memory aid: eXtract Ze Files = `xzf`. Use `tar -tzf src.tar.gz` to peek inside without extracting." },
      { learn: "`zip` creates compressed archives that work everywhere — Windows, Mac, Linux. `-r` is required for directories (recursive). The difference from `tar.gz`: zip is more portable, tar.gz is more common on Linux servers.", task: "Compress the `project/` directory into `project.zip`", hint: "zip -r project.zip project/", accept: ["zip -r project.zip project/", "zip -r project.zip project"], output: "  adding: project/ (stored 0%)\n  adding: project/src/ (stored 0%)\n  adding: project/src/train.py (deflated 62%)\n  adding: project/README.md (deflated 45%)", note: "Extract with `unzip project.zip`. List contents with `unzip -l project.zip`. Use `zip` when sharing with non-Linux users, `tar.gz` for server backups." },
      { learn: "`unzip` extracts `.zip` archives. Unlike `tar`, the command name is different for creating (`zip`) and extracting (`unzip`). Use `-l` to list contents without extracting, or `-d` to extract to a specific directory.", task: "Extract `project.zip`", hint: "unzip project.zip", accept: ["unzip project.zip"], output: "Archive:  project.zip\n   creating: project/\n   creating: project/src/\n  inflating: project/src/train.py\n  inflating: project/README.md", note: "Peek inside first with `unzip -l project.zip`. Extract to a specific folder with `unzip project.zip -d /tmp/`. Quick comparison: `tar -xzf` for `.tar.gz`, `unzip` for `.zip`." },
      { learn: "`vim` is a powerful text editor in the terminal. It has two main modes: Normal mode (for navigating) and Insert mode (for typing). Press `i` to enter Insert mode, `Esc` to return to Normal mode, `:wq` to save and quit, `:q!` to quit without saving. Learn interactively at openvim.com. Another common editor is `nano`.", task: "Open `todo.txt` in vim", hint: "vim todo.txt", accept: ["vim todo.txt", "vi todo.txt"], output: "  ~ VIM - todo.txt\n\n  Buy groceries\n  Fix deploy script\n  Review PR #42\n\n  i:insert  Esc:normal  :wq:save+quit  :q!:quit", note: "Vim is everywhere — every server has it. The learning curve is steep but worth it. Start with `vimtutor` on any terminal, or practice at openvim.com" },
      { learn: "`curl` transfers data to and from URLs — it's the Swiss army knife for working with APIs and web services. Key flags: `-X` sets the HTTP method (GET, POST, PUT, DELETE), `-d` sends data in the request body, `-H` sets headers (like telling the server you're sending JSON), `-s` suppresses the progress bar, and `-o` saves the response to a file.", task: "Run `curl -X POST -d '{\"text\":\"hello\"}' -H 'Content-Type: application/json' localhost:8000/predict`", hint: "curl -X POST -d '{\"text\":\"hello\"}' -H 'Content-Type: application/json' localhost:8000/predict", accept: ["curl -X POST -d '{\"text\":\"hello\"}' -H 'Content-Type: application/json' localhost:8000/predict", "curl -X POST localhost:8000/predict"], output: "{\"prediction\":\"positive\",\"confidence\":0.94,\"latency_ms\":245}", note: "`curl` is essential for testing APIs. Try: `curl -s url | jq .` (pretty-print JSON), `curl -v url` (show full request/response headers for debugging), `curl -o file.zip url` (download to file)." },
      { learn: "`wget` downloads files from the internet. While `curl` is better for API interactions, `wget` is designed specifically for downloading: it handles retries automatically, can resume interrupted downloads with `-c`, and can even download entire websites recursively. Use `wget` when you need to grab a file, `curl` when you need to talk to an API.", task: "Run `wget https://example.com/data.csv` to download the file", hint: "wget https://example.com/data.csv", accept: ["wget https://example.com/data.csv"], output: "data.csv            100%[=================>]  2.4M  1.2MB/s    in 2.0s\n\n'data.csv' saved [2400000/2400000]", note: "`wget` saves to the current directory. `-O name.txt` renames the download. `-q` for quiet mode. `-c` resumes interrupted downloads — great for large files over flaky connections. Note: macOS doesn't include `wget` by default — install with `brew install wget`, or use `curl -O url` as an alternative." },
    ],
  },
  {
    id: "read", title: "Reading & Searching", icon: "🔍", color: "#60a5fa", difficulty: "beginner",
    desc: "Read files, search content, watch logs in real time",
    badge: "Inspector",
    teach: [
      "Most of your time on the command line is spent reading — log files, configs, data files, source code. Linux gives you a toolkit of specialized readers, each designed for a different situation.",
      "Use `cat` for short files, `less` when you need to scroll and search, `head`/`tail` to peek at the start or end, and `tail -f` to watch files update in real time (perfect for monitoring training or server logs).",
      "`grep` is one of the most powerful commands you'll learn. It searches for text patterns inside files — finding errors in logs, tracing function calls in code, or filtering data. Combined with flags like `-r` (recursive), `-i` (case-insensitive), and `-c` (count), it becomes your primary search tool.",
      "`wc` (word count) rounds out the toolkit — counting lines, words, and characters. Together, these commands let you investigate any file on any server. Remember: `man [command]` is always available for the full reference, and these commands all work the same in your own terminal.",
    ],
    steps: [
      { learn: "`cat` prints an entire file to the terminal. Syntax: `cat [filename]`. Great for short files like configs.", task: "Use `cat` to read the file `config/model.yaml`", hint: "cat config/model.yaml", accept: ["cat config/model.yaml"], output: "model:\n  name: transformer-v2\n  hidden_size: 768\n  num_layers: 12\n  dropout: 0.1\n\ntraining:\n  batch_size: 64\n  learning_rate: 0.0001\n  epochs: 50", note: "Quick and clean. Try: `cat -n file.txt` (adds line numbers). For large files, `cat` floods your terminal — use `less` instead. Open your own terminal and try `cat` on a real file — the commands here all work the same way." },
      { learn: "`less` opens a file for scrollable reading. Syntax: `less [filename]`. Inside `less`: `/` to search, `n` for next match, `q` to quit.", task: "Use `less` to open `training.log` for scrollable reading", hint: "less training.log", accept: ["less training.log"], output: "Epoch 1/50 - loss: 2.4532 - acc: 0.342\nEpoch 2/50 - loss: 1.8923 - acc: 0.451\nEpoch 3/50 - loss: 1.5234 - acc: 0.523\n...\n\n  / to search · n for next · q to quit", note: "`less` lets you scroll up/down, search with `/`, and doesn't pollute your history. Try: `less +F file.log` (follows like `tail -f`, `Ctrl+C` to stop following)." },
      { learn: "`head` shows the first N lines of a file. Syntax: `head -n [number] [filename]`. Essential for checking CSV headers.", task: "Use `head -n 5` to show the first 5 lines of `data/train.csv`", hint: "head -n 5 data/train.csv", accept: ["head -n 5 data/train.csv", "head -5 data/train.csv"], output: "id,text,label,timestamp\n1,\"Earnings exceeded expectations\",positive,2026-01-15\n2,\"Revenue declined third quarter\",negative,2026-01-15\n3,\"New strategic partnership\",positive,2026-01-16\n4,\"2000 employees affected\",negative,2026-01-16", note: "First line is headers, rest is data. Try: `head -n 1 file.csv` (just headers), `head -c 100 file` (first 100 bytes). You'll do this every time you get a new dataset." },
      { learn: "`tail` shows the last N lines. Syntax: `tail -n [number] [filename]`. Without `-n`, defaults to last 10 lines.", task: "Use `tail -n 3` to show the last 3 lines of `training.log`", hint: "tail -n 3 training.log", accept: ["tail -n 3 training.log", "tail -3 training.log"], output: "Epoch 48/50 - loss: 0.0212 - acc: 0.990\nEpoch 49/50 - loss: 0.0198 - acc: 0.991\nEpoch 50/50 - loss: 0.0187 - acc: 0.992", note: "Quick way to see recent output. Try: `tail -n 50 file` (last 50 lines), `tail -f file` (follow in real time — covered next)." },
      { learn: "`tail -f` follows a file in real time — new lines appear as they're written. Syntax: `tail -f [filename]`. `Ctrl+C` to stop.", task: "Use `tail -f` to follow `training.log` in real time", hint: "tail -f training.log", accept: ["tail -f training.log"], output: "Epoch 48/50 - loss: 0.0212 - acc: 0.990\nEpoch 49/50 - loss: 0.0198 - acc: 0.991\nEpoch 50/50 - loss: 0.0187 - acc: 0.992\n█ watching for new lines... (Ctrl+C to stop)", note: "You'll use `tail -f` daily for training progress, API logs, system events. Try: `tail -f -n 50 file` (start from last 50 then follow)." },
      { learn: "`diff` compares two files line by line and shows what changed. Essential for reviewing config changes or comparing outputs.", task: "Use `diff` to compare `config.old.yaml` and `config.yaml`", hint: "diff config.old.yaml config.yaml", accept: ["diff config.old.yaml config.yaml", "diff config.yaml config.old.yaml"], output: "3c3\n< hidden_size: 512\n---\n> hidden_size: 768\n5c5\n< dropout: 0.2\n---\n> dropout: 0.1", note: "`<` = old file, `>` = new file. Try: `diff -u` for unified format (like git diffs), `diff -r dir1/ dir2/` to compare entire directories." },
      { learn: "`grep` searches for a pattern inside files. Syntax: `grep [pattern] [filename]`. It prints every line that matches. When your search pattern is a single word (no spaces), quotes are optional. When it contains spaces, you must wrap it in quotes so the shell treats it as one argument — e.g. `grep \"out of memory\" app.log`.", task: "Use `grep` to search for 'CUDA' in `training.log`", hint: "grep CUDA training.log", accept: ["grep CUDA training.log", "grep \"CUDA\" training.log", "grep 'CUDA' training.log"], output: "Epoch 23 - CUDA error: out of memory\nEpoch 23 - CUDA: tried to allocate 2.4GB\nEpoch 38 - CUDA warning: fragmentation", note: "`grep` is your search engine for files. No quotes needed here because `CUDA` is one word. Try: `grep -n` (show line numbers), `grep -v` (invert — show non-matching lines), `grep --color` (highlight matches)." },
      { learn: "`grep -i` does case-insensitive search. Syntax: `grep -i [pattern] [filename]`. Catches ERROR, Error, and error.", task: "Use `grep -i` to search for 'error' (any case) in `app.log`", hint: "grep -i error app.log", accept: ["grep -i error app.log", "grep -i \"error\" app.log", "grep -i 'error' app.log"], output: "10:42:05 ERROR: Connection timeout to database\n10:42:07 ERROR: CUDA out of memory\n10:42:08 ERROR: Connection timeout to database\n10:42:10 ERROR: Invalid input format\n10:42:11 ERROR: Connection timeout to database\n10:42:13 ERROR: CUDA out of memory\n10:42:14 ERROR: Connection timeout to database", note: "`-i` catches ERROR, Error, error — all of them. Try: `grep -in` (case-insensitive + line numbers). Essential when searching mixed-case logs." },
      { learn: "`grep -c` counts matching lines instead of printing them. Syntax: `grep -c [pattern] [filename]`.", task: "Use `grep -c` to count how many ERROR lines are in `app.log`", hint: "grep -c ERROR app.log", accept: ["grep -c ERROR app.log", "grep -c \"ERROR\" app.log", "grep -c 'ERROR' app.log"], output: "7", note: "7 errors. Faster than `grep | wc -l`. Try: `grep -ci ERROR file` (case-insensitive count), `grep -c pattern *.log` (count per file)." },
      { learn: "`grep -r` searches recursively through all files in a directory. Add `-n` for line numbers. Syntax: `grep -rn [pattern] --include=[glob] [path]`. Since `import torch` has a space, you need quotes around it — otherwise the shell thinks `torch` is a separate argument.", task: "Use `grep -rn` to search for `import torch` in all `.py` files under `.`", hint: "grep -rn \"import torch\" --include=\"*.py\" .", accept: ["grep -rn \"import torch\" --include=\"*.py\" .", "grep -rn 'import torch' --include='*.py' .", "grep -r \"import torch\" --include=\"*.py\" .", "grep -r 'import torch' --include='*.py' ."], output: "./src/train.py:1:import torch\n./src/train.py:2:import torch.nn as nn\n./src/inference.py:1:import torch\n./src/utils.py:3:from torch.utils.data import DataLoader", note: "`grep -rl` shows filenames only, `grep -rn` adds line numbers. Try this on a real project in your own terminal — `grep -rn` is how experienced developers navigate unfamiliar codebases. Run `man grep` to discover more flags." },
      { learn: "`wc` counts words (`-w`), lines (`-l`), characters (`-c`), or bytes. Without flags it shows all three. `-l` (lines) is by far the most common — it's how you check dataset sizes, log lengths, and count results.", task: "Use `wc -l` to count the lines in `data/train.csv`", hint: "wc -l data/train.csv", accept: ["wc -l data/train.csv"], output: "  248501 data/train.csv", note: "248,501 lines including header = 248,500 rows. Try: `wc -w` (word count), `wc -c` (byte count), `wc -l *.csv` (count all CSVs)." },
    ],
  },
  {
    id: "pipe", title: "Pipes & Processing", icon: "🔗", color: "#c084fc", difficulty: "intermediate",
    desc: "Chain commands together to transform data",
    badge: "Pipe Master",
    teach: [
      "Pipes are the core idea that makes Unix powerful. Instead of one giant program that does everything, you chain small, focused tools together — each one transforms the data and passes it along.",
      "The pipe symbol `|` sends the output of one command as input to the next. `>` sends output to a file, `>>` appends. These two operators — pipe and redirect — are how you route data through the shell.",
      "You'll learn to build processing chains: filter with `grep`, extract columns with `awk` or `cut`, sort and deduplicate with `sort`/`uniq`, and substitute text with `sed`. Each tool does one thing well.",
      "The key habit: build chains incrementally. Start with one command, check the output, then add `| next_command`. This way you see exactly what each step does before adding the next. Pipes are best learned with real data — try these on your own machine with real files.",
    ],
    steps: [
      { learn: "`>` redirects output to a file (overwrites). `>>` appends instead. These are fundamental — half of shell scripting is redirecting output.", task: "Use `>` to write 'hello world' into a file called `greeting.txt`", hint: "echo hello world > greeting.txt", accept: ["echo hello world > greeting.txt", "echo 'hello world' > greeting.txt", 'echo "hello world" > greeting.txt'], output: "", note: "`>` creates or overwrites the file. To add without overwriting: `echo 'line two' >> greeting.txt`. This is how you build log files, save results, and write configs from scripts." },
      { learn: "`>>` appends to a file instead of overwriting. Use `>` for fresh files, `>>` for accumulating data.", task: "Use `>>` to append the word 'goodbye' to `greeting.txt`", hint: "echo goodbye >> greeting.txt", accept: ["echo goodbye >> greeting.txt", "echo 'goodbye' >> greeting.txt"], output: "", note: "`greeting.txt` now has two lines. Common pattern: run commands in a loop, `>>` results to a file. Also: `command > output.log 2>&1` (redirect both stdout and stderr)." },
      { learn: "The pipe symbol `|` sends output from one command as input to the next. Syntax: `[command1] | [command2]`. This is the core Unix idea.", task: "Pipe 'hello world' into `wc -w` to count the words", hint: "echo hello world | wc -w", accept: ["echo hello world | wc -w", 'echo "hello world" | wc -w', "echo 'hello world' | wc -w"], output: "2", note: "`echo` produced 'hello world', pipe sent it to `wc -w` which counted 2 words. Try: `echo hello | wc -c` (count characters). This pattern scales to any chain." },
      { learn: "`sort` orders lines. Syntax: `[command] | sort [flags]`.  Sorting is alphabetical but adding `-n` sorts numerically, `-r` reverses, and `-u` removes duplicates.", task: "Pipe `ls` into `sort` to list files alphabetically", hint: "ls | sort", accept: ["ls | sort"], output: "app.log\nconfig\ndata\nDockerfile\ndocker-compose.yml\nmodels\nrequirements.txt\nsrc\ntraining.log", note: "Sorted A-Z. Try: `ls -l | sort -k5 -n` (sort by size column), `sort -t',' -k2 file.csv` (sort CSV by 2nd column)." },
      { learn: "`uniq` removes duplicate lines — but there's a catch: it only removes duplicates that are right next to each other. That's why you always `sort` first, so identical lines end up together. Adding `-c` to make `uniq -c` also counts how many times each line appeared. To complete this step you'll need `grep` and piping.", task: "Count the different types of ERROR in `app.log`", hint: "grep ERROR app.log | sort | uniq -c", accept: ["grep ERROR app.log | sort | uniq -c"], output: "      8 ERROR: Checkpoint not found\n     47 ERROR: Connection timeout\n     23 ERROR: CUDA out of memory\n     12 ERROR: Invalid input\n      5 ERROR: Rate limit exceeded", note: "We can see the counts, but they're in alphabetical order. Connection timeout has 47 occurrences but it's not at the top. Next we'll sort by count to rank them." },
      { learn: "To rank results by count, pipe into `sort -rn` — that's numeric sort (`-n`) in reverse/descending order (`-r`), so the biggest numbers come first. Add `head -5` to cap at the top 5. This full chain is called the frequency count pattern: `sort | uniq -c | sort -rn`. It works on anything — log errors, IP addresses, usernames, status codes. Worth memorizing.", task: "Find the top 5 most frequent errors in `app.log`", hint: "grep ERROR app.log | sort | uniq -c | sort -rn | head -5", accept: ["grep ERROR app.log | sort | uniq -c | sort -rn | head -5", "grep ERROR app.log | sort | uniq -c | sort -rn | head"], output: "     47 ERROR: Connection timeout\n     23 ERROR: CUDA out of memory\n     12 ERROR: Invalid input\n      8 ERROR: Checkpoint not found\n      5 ERROR: Rate limit exceeded", note: "Connection timeout is the #1 issue. Try this pattern on a real log file in your own terminal — it's one of the most useful things you'll learn." },
      { learn: "`awk` is a powerful text-processing tool. Its most common use is extracting columns — it splits each line by whitespace and numbers them: `$1` is the first column, `$2` the second, `$NF` the last. But `awk` can do much more: filter rows (`awk '$9 >= 400'` to show only HTTP errors), do math (`awk '{sum += $5} END {print sum}'` to total a column), and reformat output. It's essentially a mini programming language for text. To extract a column, use `awk '{print $N}' [file]` where `$N` is the column number. Pipe to `head -5` to see just the first 5 lines.", task: "Extract the IP addresses (column 1) from `access.log`", hint: "awk '{print $1}' access.log | head -5", accept: ["awk '{print $1}' access.log | head -5", "awk '{print $1}' access.log"], output: "203.45.67.89\n203.45.67.89\n10.0.1.15\n185.22.33.44\n203.45.67.89", note: "Print multiple columns: `awk '{print $1, $9}'` gives IP + status code. Filter rows: `awk '$9 >= 400'` shows only errors. Sum a column: `awk '{s+=$5} END {print s}'`. For CSVs, change the delimiter: `awk -F',' '{print $2}' data.csv`. Run `man awk` to go deeper." },
      { learn: "`cut` extracts columns from structured text. `-d` sets the delimiter (the character separating columns) and `-f` picks which column number to extract. For a CSV file, columns are separated by commas, so `-d','` means 'split on commas' and `-f2` means 'give me the 2nd column'.", task: "Extract the text column (column 2) from `data/train.csv`", hint: "cut -d',' -f2 data/train.csv | head -5", accept: ["cut -d',' -f2 data/train.csv | head -5", "cut -d, -f2 data/train.csv | head -5", "cut -d',' -f2 data/train.csv", "cut -d, -f2 data/train.csv"], output: "text\n\"Earnings exceeded expectations\"\n\"Revenue declined third quarter\"\n\"New strategic partnership announced\"\n\"2000 employees affected by layoffs\"", note: "Column 1 is `id`, column 2 is `text`, column 3 is `label`. Try: `cut -d':' -f1 /etc/passwd` on your machine to extract usernames. Use `awk` when you need more complex logic." },
      { learn: "Normally when you redirect output to a file with `>`, you can't see it on screen anymore. `tee` solves this — it takes input from a pipe and writes it to both the screen AND a file at the same time. Syntax: `command | tee [filename]`. This is useful when you want to watch a long-running process live while also saving the output for later.", task: "Run `python train.py` and save the output to `training.log` while watching it live", hint: "python train.py | tee training.log", accept: ["python train.py 2>&1 | tee training.log", "python train.py | tee training.log"], output: "Starting training...\nEpoch 1/50 - loss: 2.4532\nEpoch 2/50 - loss: 1.8923\n  (output → screen AND training.log)", note: "The output appears on screen AND gets saved to `training.log`. Use `tee -a file` to append instead of overwriting. `tee` is named after a T-shaped pipe fitting — the data splits two ways." },
      { learn: "`sed` does find/replace on text. Syntax: `sed 's/[old]/[new]/g' [file]`. `s` = substitute, `g` = all occurrences per line.", task: "Use `sed` to replace 'localhost' with '0.0.0.0' in `config/server.yaml`", hint: "sed 's/localhost/0.0.0.0/g' config/server.yaml", accept: ["sed 's/localhost/0.0.0.0/g' config/server.yaml"], output: "server:\n  host: 0.0.0.0\n  port: 8000\n  db_host: 0.0.0.0:5432", note: "Without `-i`, `sed` just previews. Try: `sed -i 's/old/new/g' file` (edit in place), `sed '3d' file` (delete line 3). `sed` is deep — run `man sed` or search 'sed tutorial' online to learn regex patterns and advanced substitutions. Note: on macOS, `sed -i` requires an empty string argument: `sed -i '' 's/old/new/g' file`. This is a common gotcha when switching between macOS and Linux." },
      { learn: "`xargs` converts piped input into arguments for another command. Syntax: `[command] | xargs [command2]`. Bridges commands that don't read stdin.", task: "Use `find . -name \"*.pyc\"` piped into `xargs rm` to delete all `.pyc` files", hint: "find . -name \"*.pyc\" | xargs rm", accept: ["find . -name \"*.pyc\" | xargs rm", "find . -name '*.pyc' | xargs rm"], output: "", note: "Try: `find . -name '*.log' | xargs wc -l` (count lines in all logs), `echo 'file1 file2' | xargs cat`. `xargs` is the bridge between `find` and any action." },
      { learn: "`&&` runs the next command only if the first succeeds. `||` runs it only if the first fails. Chain commands for conditional execution.", task: "Use `&&` to create a directory called `deploy` and `cd` into it in one line", hint: "mkdir deploy && cd deploy", accept: ["mkdir deploy && cd deploy"], output: "", note: "`&&` = 'and then'. `mkdir fail || echo 'failed'` = 'or else'. Common: `npm install && npm start`, `make && make install`. Use `&&` to chain dependent commands safely." },
      { learn: "Chain multiple pipes for real analysis. Each `|` adds a transformation step. We'll count unique error types in `app.log` by filtering with `grep`, deduplicating with `sort -u`, and counting with `wc -l`.", task: "Grep for `ERROR` in `app.log`, pipe through `sort -u`, then `wc -l` to count unique error types", hint: "grep ERROR app.log | sort -u | wc -l", accept: ["grep ERROR app.log | sort -u | wc -l"], output: "4", note: "Three tools chained: `grep` filters, `sort -u` deduplicates, `wc -l` counts. Try building it step by step: run `grep` first, then add `| sort -u`, then add `| wc -l`." },
    ],
  },
  {
    id: "sys", title: "System Processes", icon: "📊", color: "#f97316", difficulty: "intermediate",
    desc: "Monitor resources, manage processes, diagnose problems",
    badge: "Sysadmin",
    teach: [
      "When you SSH into a server for the first time — or when something goes wrong — you need to understand the machine you're on. How much RAM? How much disk space? What's running? What's eating CPU?",
      "This chapter teaches the commands that sysadmins and DevOps engineers run first: `free` for memory, `df` for disk, `htop` for CPU/processes, `nvidia-smi` for GPUs. These are your diagnostic tools.",
      "Every running program is a process with a unique PID (process ID). `ps aux` lists them all. `kill` stops them. `Ctrl+C` interrupts the foreground process, `Ctrl+Z` suspends it, `bg`/`fg` manage background jobs.",
      "You'll also learn environment variables (`$PATH`, `$HOME`) and process management (`nohup`, `&`) — essential for running long training jobs or keeping services alive.",
    ],
    steps: [
      { learn: "`uname -a` shows basic system information: OS, kernel version, architecture.", task: "Run `uname -a` to print system information", hint: "uname -a", accept: ["uname -a"], output: "Linux gpu-box 5.15.0-91-generic #101-Ubuntu SMP x86_64 GNU/Linux", note: "Linux, kernel 5.15, 64-bit (x86_64). This tells you the OS and architecture. `uname -r` shows just the kernel version." },
      { learn: "`free -h` shows memory in human readable format. Check 'available', not 'free' since Linux caches aggressively.", task: "Run `free -h` to check available RAM", hint: "free -h", accept: ["free -h"], output: "              total    used    free   available\nMem:           61Gi   22.1Gi   30Gi      37Gi\nSwap:            0B      0B      0B", note: "37GB available. Linux caches disk data in RAM (buff/cache) but releases it when apps need it. No swap means OOM kills are instant. Note: `free` is Linux only. On macOS, use `vm_stat` for memory info or check Activity Monitor." },
      { learn: "`df -h` shows disk usage for all mounted filesystems. `-h` makes sizes human readable (GB/MB instead of blocks). When a disk fills up, things break silently — this is how you catch it.", task: "Run `df -h` to check disk space on all drives", hint: "df -h", accept: ["df -h"], output: "Filesystem      Size  Used Avail Use% Mounted on\n/dev/nvme0n1p1  200G   42G  158G  21% /\n/dev/nvme1n1    500G  312G  188G  63% /data", note: "`/data` is 63% full. When this hits 100%, things break silently. Set alerts at 80%." },
      { learn: "`nvidia-smi` shows GPU model, memory, temperature, utilisation, and running processes.", task: "Run `nvidia-smi` to check GPU status and memory", hint: "nvidia-smi", accept: ["nvidia-smi"], output: "| GPU  Name       Mem-Usage       GPU-Util |\n|  0   A100 80GB  12040/81920 MiB    67%   |\n|  1   A100 80GB      0/81920 MiB     0%   |\n|------------------------------------------|\n| PID   GPU  Mem      Command              |\n| 12345  0   12040MiB python train.py      |", note: "GPU 0 is training (12GB, 67%). GPU 1 is idle. `watch -n 2 nvidia-smi` refreshes live. You'll run this constantly." },
      { learn: "`ps aux` lists every running process. `ps` = process status, `a` = all users, `u` = user-oriented format, `x` = include processes without a terminal. The most common pipe pattern: `ps aux | grep [name]` — list all processes, then filter.", task: "Pipe `ps aux` into `grep python` to find Python processes", hint: "ps aux | grep python", accept: ["ps aux | grep python"], output: "ubuntu  12345  45.2  8.3 python train.py --epochs 50\nubuntu  12478   0.3  1.2 python inference.py --port 8000\nubuntu  12901   0.0  0.0 grep python", note: "`ps aux` lists processes, `grep` filters. The last line is grep itself (ignore it). Try: `ps aux | grep -v grep | grep python` (excludes the grep line)." },
      { learn: "`htop` is an interactive process viewer. Sort by CPU or memory, filter, kill processes visually.", task: "Run `htop` to open the interactive process monitor", hint: "htop", accept: ["htop", "top"], output: "CPU[||||||||||||       34.2%]  Mem[||||||||||||||  14.2G/61G]\n\n  PID  USER    CPU%  MEM%  COMMAND\n12345  ubuntu  45.2   8.3  python train.py\n12478  ubuntu   0.3   1.2  python inference.py\n 1234  root     0.1   0.0  nginx: master\n\n  F6:sort  F9:kill  q:quit", note: "CPU bars at top, processes below. `F6` sorts by any column. `F9` kills a process. `q` quits. Install with `sudo apt install htop` (Linux) or `brew install htop` (macOS) — it's not pre-installed on most systems." },
      { learn: "`kill PID` sends a polite stop signal. `kill -9 PID` forces immediate termination.", task: "Use `kill` to stop process `12345` (the training job)", hint: "kill 12345", accept: ["kill 12345"], output: "", note: "Plain `kill` lets the process save state. `kill -9` is instant, no cleanup. Use `-9` only when regular `kill` doesn't work." },
      { learn: "`ping` sends small packets to a server to check if it's reachable and how fast the connection is. It runs continuously until you stop it — which makes it perfect for learning process control. Try it on your own machine with `ping google.com`.", task: "Run `ping google.com` to test the connection", hint: "ping google.com", accept: ["ping google.com"], output: "PING google.com (142.250.80.46): 56 bytes\n64 bytes from 142.250.80.46: time=12.3 ms\n64 bytes from 142.250.80.46: time=11.8 ms\n64 bytes from 142.250.80.46: time=12.1 ms\n  (running continuously...)", note: "Ping runs forever until you stop it. Your terminal is blocked — you can't type anything else. Let's fix that." },
      { learn: "`Ctrl+C` sends an interrupt signal that terminates the current command. Use it to stop any long-running process like `ping`, `tail -f`, or `watch`. It kills the process — it won't resume.", task: "Press `Ctrl+C` to stop the ping", hint: "Ctrl+C", accept: ["ctrl+c", "Ctrl+C", "ctrl+C"], output: "^C\n--- google.com ping statistics ---\n3 packets transmitted, 3 received, 0% packet loss", note: "`Ctrl+C` is your emergency stop. You'll use it dozens of times a day. Any time a command is taking too long or you ran the wrong thing — `Ctrl+C` to kill it." },
      { learn: "Let's ping again. This time, instead of killing it, we'll learn to suspend it — pausing the process without stopping it.", task: "Run `ping google.com` again", hint: "ping google.com", accept: ["ping google.com"], output: "PING google.com (142.250.80.46): 56 bytes\n64 bytes from 142.250.80.46: time=12.3 ms\n64 bytes from 142.250.80.46: time=11.8 ms\n  (running continuously...)", note: "Terminal is blocked again. But this time we won't kill it — we'll pause it." },
      { learn: "`Ctrl+Z` suspends the current foreground process. Unlike `Ctrl+C` which kills it, `Ctrl+Z` just pauses it — the process is still alive and can be resumed later.", task: "Press `Ctrl+Z` to suspend the ping", hint: "Ctrl+Z", accept: ["ctrl+z", "Ctrl+Z", "ctrl+Z"], output: "[1]+  Stopped                 ping google.com", note: "The ping is paused, not dead. Your terminal is free again. The `[1]` is the job number — you'll use it with `bg` and `fg` next." },
      { learn: "The ping is now suspended. `jobs` lists all suspended and background processes in your current shell session. Let's confirm it's there.", task: "Type `jobs` to see the suspended process", hint: "jobs", accept: ["jobs"], output: "[1]+  Stopped                 ping google.com", note: "There it is — job `[1]`, stopped. The `+` marks the most recent job. You can control it using its job number with `%1`." },
      { learn: "The ping is paused but still alive. `bg` resumes it in the background — it keeps running but gives you your terminal back. Use `bg %1` where `%1` refers to job number 1 from the `jobs` list.", task: "Resume the ping in the background with `bg %1`", hint: "bg %1", accept: ["bg %1", "bg"], output: "[1]+ ping google.com &", note: "The ping is now running in the background — notice the `&` at the end. Your terminal is free. The process continues silently." },
      { learn: "Now let's bring it back. `fg` moves a background process to the foreground so you can interact with it again. This completes the cycle: `Ctrl+Z` to pause, `bg` to background, `fg` to foreground.", task: "Bring the ping back to the foreground with `fg %1`", hint: "fg %1", accept: ["fg %1", "fg"], output: "ping google.com\n64 bytes from 142.250.80.46: time=12.4 ms\n64 bytes from 142.250.80.46: time=11.9 ms\n  (running in foreground...)", note: "The ping is back in the foreground and your terminal is blocked again. Let's stop it for good this time." },
      { learn: "The ping is running in the foreground again. Now use `Ctrl+C` to terminate it completely. This is the full process control cycle: start a process, `Ctrl+Z` to suspend, `bg` to background, `fg` to foreground, `Ctrl+C` to kill.", task: "Press `Ctrl+C` to stop the ping for good", hint: "Ctrl+C", accept: ["ctrl+c", "Ctrl+C", "ctrl+C"], output: "^C\n--- google.com ping statistics ---\n12 packets transmitted, 12 received, 0% packet loss", note: "Done. You now know the full cycle: `Ctrl+Z` (pause) -> `bg` (background) -> `fg` (foreground) -> `Ctrl+C` (kill). This is essential when you forget to run something with `&`. For long-running work, `tmux` (covered later) is even better." },
      { learn: "Instead of starting a process and then backgrounding it, you can run it in the background from the start by adding `&` at the end. Your terminal stays free immediately. But be aware: background processes still die if you close the terminal.", task: "Run `python train.py &` to start training in the background", hint: "python train.py &", accept: ["python train.py &"], output: "[1] 13579", note: "Process 13579 runs in background. You can keep working. But if you log out or close the terminal, the process dies. Note: background processes still print output to your screen, which can look confusing — it seems blocked but your prompt is actually free. `Ctrl+C` won't stop a background process. Use `fg` to bring it foreground first, or `kill %1` to stop it directly." },
      { learn: "`nohup` (no hangup) solves that problem — it makes a process survive even after you disconnect. Combined with `&`, it runs in the background AND survives logout. Breaking down the full command: `nohup` keeps it alive after logout, `> train.log` saves normal output to a file, `2>&1` also sends error messages to that same file (2 = errors, 1 = normal output, `>&` means 'redirect to'), and `&` backgrounds it.", task: "Run `nohup python train.py > train.log 2>&1 &`", hint: "nohup python train.py > train.log 2>&1 &", accept: ["nohup python train.py > train.log 2>&1 &", "nohup python train.py &"], output: "[1] 13580\nnohup: appending output to 'train.log'", note: "Survives logout, output saved to `train.log`. Without `2>&1`, errors would still print to your screen instead of the file. `tmux` is even better for long-running work — you can reattach and see output live. But `nohup` is useful for quick fire-and-forget tasks." },
      { learn: "`lsof -i :PORT` shows what process is using a port. Essential when you get 'address already in use'.", task: "Run `lsof -i :8000` to find what's using port 8000", hint: "lsof -i :8000", accept: ["lsof -i :8000"], output: "COMMAND   PID    USER  TYPE  NAME\npython    12478  ubuntu IPv4  *:8000 (LISTEN)", note: "PID 12478 is your culprit. `kill 12478` to free the port. `kill -9 12478` to force it." },
      { learn: "`uptime` shows how long the system has been running and the load average.", task: "Run `uptime` to check how long the server has been running", hint: "uptime", accept: ["uptime"], output: " 10:42:13 up 14 days, 3:22, 2 users, load average: 0.42, 0.38, 0.35", note: "Load average: 1-min, 5-min, 15-min. Values below CPU count = healthy. Above = overloaded. This box has been up 14 days." },
      { learn: "`watch` repeats a command every N seconds so you can monitor it live. Syntax: `watch -n [seconds] [command]`.", task: "Use `watch -n 2` to monitor `df -h` every 2 seconds", hint: "watch -n 2 df -h", accept: ["watch -n 2 df -h", "watch df -h"], output: "Every 2.0s: df -h\n\nFilesystem      Size  Used Avail Use% Mounted on\n/dev/nvme0n1p1  200G   42G  158G  21% /\n/dev/nvme1n1    500G  312G  188G  63% /data\n\n  (refreshing every 2s — Ctrl+C to stop)", note: "`watch` is like having a live dashboard. Try it on your own machine: `watch -n 2 df -h` or `watch -n 1 date`. `Ctrl+C` to stop. Note: macOS doesn't include `watch` by default — install with `brew install watch`." },
    ],
  },
  {
    id: "git", title: "Version Control", icon: "🌿", color: "#f472b6", difficulty: "intermediate",
    desc: "Track changes, collaborate, and manage code history",
    badge: "Version Controller",
    teach: [
      "`git` is the version control system used by virtually every software project. It tracks changes to your files over time, lets you undo mistakes, create branches for experiments, and collaborate with others without overwriting each other's work.",
      "The core workflow is: edit files → `git add` (stage changes) → `git commit` (save a snapshot) → `git push` (share with others). `git status` shows where you are in this cycle at any time.",
      "Branches let you work on features in isolation. `git checkout -b feature` creates a new branch, you make changes, then merge back. This is how teams work on dozens of features simultaneously without conflicts.",
      "This chapter covers the essential daily commands: status, diff, add, commit, push, pull, branching, and viewing history. These commands will become muscle memory.",
    ],
    steps: [
      { learn: "`git clone` downloads a remote repository to your machine. It creates a directory with the full history.", task: "Run `git clone https://github.com/user/project.git` to download the repository", hint: "git clone https://github.com/user/project.git", accept: ["git clone https://github.com/user/project.git"], output: "Cloning into 'project'...\nremote: Enumerating objects: 847, done.\nremote: Total 847, done.\nReceiving objects: 100% (847/847), 2.4 MiB, done.", note: "You now have a full copy of the project and its entire history. `cd project` to enter it." },
      { learn: "`git init` creates a brand new git repository in the current directory. Use this when starting a project from scratch instead of cloning. It creates a hidden `.git/` folder that tracks all your changes.", task: "Run `git init` to create a new repository", hint: "git init", accept: ["git init"], output: "Initialized empty Git repository in /home/ubuntu/my-project/.git/", note: "Now you can start adding and committing files. To connect this repo to GitHub later, create a repo on GitHub and run `git remote add origin https://github.com/user/my-project.git`, then `git push -u origin main`. Most of the time you'll use `git clone` for existing projects and `git init` for new ones." },
      { learn: "`git pull` does two things in one command: it downloads new commits from the remote (`git fetch`) and then immediately merges them into your current branch (`git merge`). By contrast, `git fetch` only downloads — it updates your knowledge of what's on the remote but doesn't change your local files. Most of the time you want `git pull` because you want the latest code, not just to know it exists. Make this a habit: always pull before starting new work. If you don't, you'll be working on outdated code and run into unnecessary conflicts later.", task: "Run `git pull` to sync the latest changes", hint: "git pull", accept: ["git pull", "git pull origin main"], output: "remote: Enumerating objects: 3, done.\nFrom github.com:user/project\n   e4c2a7f..b8d3f1a  main       -> origin/main\nUpdating e4c2a7f..b8d3f1a\nFast-forward\n README.md | 4 ++--\n 1 file changed, 2 insertions(+), 2 deletions(-)", note: "`git pull` = `git fetch` + `git merge`. `git fetch` is useful when you want to see what changed on the remote before merging (e.g. `git fetch` then `git log origin/main` to preview). But day-to-day, `git pull` is what you'll use. Run it at the start of every work session and before merging branches." },
      { learn: "`git status` shows what files have changed, what's staged, and what branch you're on. Run it constantly.", task: "Run `git status` to see what's changed", hint: "git status", accept: ["git status"], output: "On branch main\nChanges not staged for commit:\n  modified:   src/train.py\n\nUntracked files:\n  results.csv", note: "`git status` is your dashboard. Red = changed but not staged. Green = staged and ready to commit. Run it before and after every operation." },
      { learn: "`git diff` shows exactly what changed in modified files, line by line. `+` = added, `-` = removed.", task: "Run `git diff` to see your changes line by line", hint: "git diff", accept: ["git diff"], output: "diff --git a/src/train.py b/src/train.py\n--- a/src/train.py\n+++ b/src/train.py\n@@ -12,7 +12,7 @@\n-    batch_size = 32\n+    batch_size = 64\n-    learning_rate = 0.001\n+    learning_rate = 0.0001", note: "`git diff` shows unstaged changes. `git diff --staged` shows what's about to be committed. Try `git diff --name-only` to see just which files changed (no content). Useful for a quick overview." },
      { learn: "`git add` stages files for the next commit. `git add .` stages everything. `git add [file]` stages specific files.", task: "Run `git add src/train.py` to stage your changes", hint: "git add src/train.py", accept: ["git add src/train.py", "git add ."], output: "", note: "Staging is a prep step — you choose which changes to include in the commit. `git add -p` lets you stage specific lines within a file." },
      { learn: "`git commit -m` saves a snapshot of all staged changes with a descriptive message.", task: "Run `git commit -m \"increase batch size and lower learning rate\"` to save your changes", hint: "git commit -m \"increase batch size and lower learning rate\"", accept: ["git commit -m \"increase batch size and lower learning rate\"", "git commit -m 'increase batch size and lower learning rate'"], output: "[main a3f7b2c] increase batch size and lower learning rate\n 1 file changed, 2 insertions(+), 2 deletions(-)", note: "Write clear commit messages. Bad: 'fix stuff'. Good: 'increase batch size to 64 to improve GPU utilization'. Your future self will thank you." },
      { learn: "`git log` shows the commit history. `--oneline` gives a compact view. `-n 5` limits to the last 5 commits.", task: "Run `git log --oneline -5` to view recent commits", hint: "git log --oneline -5", accept: ["git log --oneline -5", "git log --oneline", "git log -5", "git log"], output: "a3f7b2c increase batch size and lower learning rate\n8d1e4f2 add early stopping callback\nc7a9b3e fix data loader memory leak\n2f6d8a1 add tensorboard logging\ne4c2a7f initial model architecture", note: "`git log` is how you understand a project's history. Try this on a real repo in your terminal: `git log --graph --oneline` is beautiful. The Pro Git book (git-scm.com/book) is the best free resource for going deeper." },
      { learn: "`git branch` lists all branches in your repository. The current branch is marked with `*`. Branches let you work on features or fixes in isolation — your changes don't affect `main` until you merge them.", task: "Run `git branch` to see all branches", hint: "git branch", accept: ["git branch", "git branch -a"], output: "* main\n  feature/data-pipeline\n  fix/memory-leak", note: "You're on `main` (the `*`). Two other branches exist. `git branch -a` also shows remote branches. `git branch -d [name]` deletes a branch you're done with." },
      { learn: "`git checkout -b [name]` creates a new branch and switches to it. Newer versions of git also support `git switch -c [name]` which does the same thing — `switch` was added to make the command clearer since `checkout` does many different things. Both work the same way.", task: "Run `git checkout -b feature/new-model` to create a branch", hint: "git checkout -b feature/new-model", accept: ["git checkout -b feature/new-model", "git switch -c feature/new-model"], output: "Switched to a new branch 'feature/new-model'", note: "You're now on `feature/new-model`. Changes here won't affect `main`. When ready, merge or create a pull request." },
      { learn: "`git push` uploads your local commits to the remote repository (usually GitHub, GitLab, or Bitbucket). `-u origin [branch]` sets up tracking so future `git push` commands know where to push. The first push to a new branch always needs `-u`. After that, just `git push` works.", task: "Run `git push -u origin feature/new-model` to push your branch to the remote", hint: "git push -u origin feature/new-model", accept: ["git push -u origin feature/new-model", "git push"], output: "Enumerating objects: 5, done.\nTo github.com:user/project.git\n * [new branch]      feature/new-model -> feature/new-model\nBranch 'feature/new-model' set up to track 'origin/feature/new-model'.", note: "Your branch is now on GitHub. Create a pull request to merge it. `git push` alone works after the first `-u` push." },
      { learn: "Before merging, you need to switch to the branch you want to merge *into*. `git merge` pulls changes into your current branch — so to get feature work into `main`, you must be on `main` first.", task: "Run `git checkout main` to switch back to the main branch", hint: "git checkout main", accept: ["git checkout main", "git switch main"], output: "Switched to branch 'main'\nYour branch is up to date with 'origin/main'.", note: "Now you're on `main`. The workflow: finish your feature → switch to main → pull latest → merge. This order prevents conflicts and keeps main clean." },
      { learn: "`git merge` combines changes from another branch into your current branch. You're on `main` now, so running `git merge feature/new-model` pulls all the commits from that feature branch into `main`. If the same lines were changed on both branches, git will flag a merge conflict for you to resolve manually.", task: "Run `git merge feature/new-model` to merge your feature branch into main", hint: "git merge feature/new-model", accept: ["git merge feature/new-model"], output: "Merge made by the 'ort' strategy.\n src/model.py    | 45 ++++++++++++++++++++++\n src/train.py    |  8 ++--\n 2 files changed, 49 insertions(+), 4 deletions(-)", note: "The feature branch changes are now in `main`. In practice, most teams merge via pull requests on GitHub/GitLab (which do `git merge` under the hood) so the code gets reviewed first. If you see 'CONFLICT' after merging, open the flagged files, resolve the conflicts, then `git add` and `git commit`." },
      { learn: "`git stash` temporarily saves all uncommitted changes and reverts your working directory to a clean state. This is useful when you need to switch branches but aren't ready to commit yet — you can't switch with unsaved changes if they'd conflict. `git stash pop` restores the changes when you're ready to pick up where you left off.", task: "Run `git stash` to save your work in progress", hint: "git stash", accept: ["git stash"], output: "Saved working directory and index state WIP on main: a3f7b2c increase batch size", note: "Your changes are safely stored. `git stash pop` brings them back, `git stash list` shows all stashes, `git stash apply` restores without deleting the stash (useful for applying to multiple branches)." },
    ],
  },
  {
    id: "ssh", title: "SSH & Remote Work", icon: "🌐", color: "#14b8a6", difficulty: "intermediate",
    desc: "Config, keys, tunnels, file transfer, and tmux sessions",
    badge: "Remote Operator",
    teach: [
      "SSH (Secure Shell) is how you securely connect to remote servers. Instead of physically sitting at a machine, you open a terminal on your laptop and SSH in — you get a full command line on the remote server, encrypted end-to-end.",
      "Every server you'll ever work on — cloud VMs, GPU clusters, production boxes — is accessed via SSH. With an SSH config file, you can replace `ssh -i ~/.ssh/key.pem ubuntu@54.123.45.67 -p 2222` with just `ssh gpu-box`.",
      "SSH keys replace passwords. You generate a key pair once (`ssh-keygen`), put the public key on the server, and you're in — no password needed. This is both more secure and more convenient.",
      "You'll also learn `scp` and `rsync` for file transfers, SSH tunneling for accessing remote services, and `tmux` for persistent terminal sessions that survive disconnections.",
    ],
    steps: [
      { learn: "Every computer on a network has an IP address (like `54.123.45.67`), and every service listens on a port number (like `22` for SSH, `80` for HTTP, `443` for HTTPS). When you SSH to a server, you're connecting to its IP address on port 22. The basic syntax is `ssh user@ip` — the user is the account on the remote machine, and the IP is its address. `localhost` or `127.0.0.1` always means 'this machine'.", task: "Run `ssh ubuntu@54.123.45.67` to connect to the remote GPU server", hint: "ssh ubuntu@54.123.45.67", accept: ["ssh ubuntu@54.123.45.67"], output: "ubuntu@gpu-box:~$", note: "You're now on the remote server. Everything you type runs on that machine, not yours. `exit` or `Ctrl+D` disconnects." },
      { learn: "Typing `ssh ubuntu@54.123.45.67` every time gets old fast. The SSH config file at `~/.ssh/config` lets you save connection details and give each server a short nickname. You add a `Host` block for each server with its details: `HostName` is the IP or domain, `User` is the login username, and `IdentityFile` points to the private key file. Once saved, you just type `ssh gpu-box` instead of the full command — SSH reads the config and fills in everything automatically. This works with all SSH-based tools: `scp`, `rsync`, tunnels, etc.", task: "Open `~/.ssh/config` with `cat ~/.ssh/config` to see the configuration", hint: "cat ~/.ssh/config", accept: ["vim ~/.ssh/config", "vi ~/.ssh/config", "nano ~/.ssh/config", "cat ~/.ssh/config"], output: "Host gpu-box\n    HostName 54.123.45.67\n    User ubuntu\n    IdentityFile ~/.ssh/gpu-key.pem\n\nHost prod\n    HostName api.mycompany.com\n    User deploy", note: "`Host gpu-box` is the alias you'll type. `HostName` is the real address. `User` is the login account. `IdentityFile` is the private key. You can also add `Port 2222` if the server uses a non-standard port. Create this file on your own machine with `vim ~/.ssh/config` and add a block for every server you connect to regularly." },
      { learn: "Now that the alias is configured, test it. `ssh gpu-box` is all you need — SSH reads `~/.ssh/config` and fills in the user, hostname, and key automatically.", task: "Run `ssh gpu-box` to connect using the alias you just configured", hint: "ssh gpu-box", accept: ["ssh gpu-box"], output: "ubuntu@gpu-box:~$", note: "Two words instead of a full IP and username. Set up a `Host` block for every server you use regularly." },
      { learn: "`ssh-keygen` generates a pair of cryptographic keys: a private key (stays on YOUR machine, never share it) and a public key (goes on servers). `ed25519` is the modern algorithm — faster and more secure than the older RSA. The keys are saved in `~/.ssh/`: the private key as `id_ed25519` and public key as `id_ed25519.pub`.", task: "Run `ssh-keygen -t ed25519` to generate a new SSH key", hint: "ssh-keygen -t ed25519", accept: ["ssh-keygen -t ed25519", "ssh-keygen"], output: "Generating public/private ed25519 key pair.\nYour identification: ~/.ssh/id_ed25519\nYour public key: ~/.ssh/id_ed25519.pub", note: "`ed25519` is faster and more secure than RSA. Never share the private key. The `.pub` file goes on servers and GitHub. Try running `ssh-keygen -t ed25519` in your own terminal — it's safe and you'll need keys eventually." },
      { learn: "`ssh-copy-id` copies your public key to a remote server's `~/.ssh/authorized_keys` file. After this, the server recognizes your key and you never need to type a password again. It's the standard way to set up key-based authentication.", task: "Run `ssh-copy-id user@server.com` to deploy your public key to the server", hint: "ssh-copy-id user@server.com", accept: ["ssh-copy-id user@server.com"], output: "Number of key(s) added: 1\nNow try: ssh user@server.com", note: "It adds your `~/.ssh/id_ed25519.pub` to the server's authorized_keys. This is a one-time setup per server." },
      { learn: "`scp` (secure copy) transfers files between your machine and a remote server over SSH. The syntax mirrors `cp`: `scp [source] [destination]`. Use `host:path` for the remote side. `-r` copies directories. Think of it as `cp` that works across machines.", task: "Run `scp model.pt gpu-box:/data/` to copy the file to the server", hint: "scp model.pt gpu-box:/data/", accept: ["scp model.pt gpu-box:/data/"], output: "model.pt                        100% 2.4GB  48.2MB/s   00:51", note: "`scp` works like `cp` but across machines. For directories add `-r`. For large transfers, `rsync` is better." },
      { learn: "SSH tunneling lets you securely access services running on a remote server (like Jupyter notebooks, databases, or web dashboards) from your local machine. The idea: the remote server is running something on a port (e.g. Jupyter on port 8888), but that port isn't accessible from the internet. SSH tunneling creates a secure bridge — it forwards traffic from a port on your laptop through the encrypted SSH connection to that port on the remote server. The syntax `-L 8888:localhost:8888` means: 'anything I send to port 8888 on my laptop, forward it to port 8888 on the remote server'.", task: "Run `ssh -L 8888:localhost:8888 gpu-box` to tunnel remote Jupyter to your local port 8888", hint: "ssh -L 8888:localhost:8888 gpu-box", accept: ["ssh -L 8888:localhost:8888 gpu-box"], output: "ubuntu@gpu-box:~$\n\n# Open http://localhost:8888", note: "Now open http://localhost:8888 in your browser — it reaches the Jupyter server running on gpu-box, securely through SSH. No ports need to be exposed to the internet. This is the standard way to access remote Jupyter, TensorBoard, Grafana, etc." },
      { learn: "The previous tunnel command also opened a full SSH shell on the remote server. Adding `-fN` runs the tunnel in the background without opening a shell. `-f` tells SSH to go to the background after connecting, and `-N` tells it not to run any remote commands — just keep the tunnel open. This is useful when you only want the tunnel (e.g. to access Jupyter in your browser) and don't need a terminal on the remote machine.", task: "Run `ssh -fNL 8888:localhost:8888 gpu-box` to tunnel in the background", hint: "ssh -fNL 8888:localhost:8888 gpu-box", accept: ["ssh -fNL 8888:localhost:8888 gpu-box"], output: "# Tunnel running silently in the background.", note: "The tunnel runs silently — your terminal is free for other work. To close it later: find the process with `ps aux | grep ssh`, then `kill` its PID. You can run multiple tunnels at once on different ports (e.g. 8888 for Jupyter, 6006 for TensorBoard)." },
      { learn: "`rsync` is like a smarter version of `scp`. Instead of copying everything every time, it compares source and destination and only transfers files that have changed. This makes it much faster for syncing large directories — the first run copies everything, but subsequent runs only send the differences. The flags: `-a` preserves permissions, timestamps, and directory structure; `-v` shows what's being transferred; `-z` compresses data during transfer to save bandwidth.", task: "Run `rsync -avz ./weights/ gpu-box:/data/weights/` to sync the folder to the server", hint: "rsync -avz ./weights/ gpu-box:/data/weights/", accept: ["rsync -avz ./weights/ gpu-box:/data/weights/"], output: "sending incremental file list\nmodel_v2.pt\n\nsent 2.4G bytes  received 35 bytes  48M bytes/sec", note: "Run the same command again and it transfers nothing — everything is already in sync. This is why `rsync` is preferred over `scp` for large directories or repeated transfers. Install with `sudo apt install rsync` or `brew install rsync` if not already available." },
      { learn: "You can run commands on a remote server without opening a shell. Just append the command after `ssh`.", task: "Run `ssh gpu-box nvidia-smi` to check GPUs remotely", hint: "ssh gpu-box nvidia-smi", accept: ["ssh gpu-box nvidia-smi"], output: "| GPU  Name       Mem-Usage       GPU-Util |\n|  0   A100 80GB  12040/81920 MiB    67%   |\n|  1   A100 80GB      0/81920 MiB     0%   |", note: "Quick remote check without opening a full session. Works with any command: `ssh prod 'docker ps'`, `ssh gpu-box 'free -h'`, etc." },
      { learn: "`ssh-add` adds your key to the SSH agent so you don't need to type the passphrase every time.", task: "Run `ssh-add ~/.ssh/id_ed25519` to add your key to the SSH agent", hint: "ssh-add ~/.ssh/id_ed25519", accept: ["ssh-add ~/.ssh/id_ed25519", "ssh-add"], output: "Identity added: /home/ubuntu/.ssh/id_ed25519", note: "The agent holds your decrypted key in memory. No more passphrase prompts until you log out." },
      { learn: "`tmux` (terminal multiplexer) is essential for remote work. Its killer feature: sessions persist even if your SSH connection drops. You start a tmux session on the server, run a long job (training, builds, deploys), and if your laptop loses WiFi or you close it, the session keeps running. You reconnect later and reattach — everything is exactly where you left it. It also lets you split your terminal into multiple panes and windows, so you can monitor logs, run commands, and watch system stats all from one SSH connection.", task: "Run `tmux new -s work` to create a named tmux session", hint: "tmux new -s work", accept: ["tmux new -s work"], output: "[work] 0:bash*\n\n# Green status bar at bottom. You're in tmux.", note: "Key commands: `Ctrl+B D` detach (session keeps running), `tmux attach -t work` reattach, `tmux ls` list sessions, `Ctrl+B %` split vertically, `Ctrl+B \"` split horizontally, `Ctrl+B C` new window. Install with `sudo apt install tmux` (Linux) or `brew install tmux` (macOS). Learn more at tmuxcheatsheet.com." },
    ],
  },
  {
    id: "docker", title: "Docker", icon: "🐳", color: "#38bdf8", difficulty: "advanced",
    desc: "Containers, images, compose, volumes, and lifecycle management",
    badge: "Container Wrangler",
    teach: [
      "Docker solves the 'it works on my machine' problem. It packages your application and all its dependencies — OS libraries, language runtimes, config files — into a container that runs identically everywhere: your laptop, a test server, or production. No more 'but I have a different Python version'.",
      "There are three key concepts: a Dockerfile is the recipe (a text file with instructions like 'start from Python 3.11, copy my code, install requirements'). An image is the snapshot built from that recipe. A container is a running instance of an image. You build once, run anywhere.",
      "For real applications, you rarely run one container alone. A web app might need an API server, a database, and a cache. Docker Compose lets you define all these services in a single `docker-compose.yml` file and manage them together with one command.",
      "Docker is the foundation of modern deployment — CI/CD pipelines build Docker images, Kubernetes orchestrates containers at scale, and most cloud services run containers under the hood.",
    ],
    steps: [
      { learn: "A `Dockerfile` is a text file that defines how to build your application's image. Each line is an instruction: `FROM` sets the base image (like `python:3.11`), `WORKDIR` sets the working directory, `COPY` adds your files, `RUN` executes commands (like installing dependencies), and `CMD` defines what runs when the container starts. Let's look at one.", task: "Run `cat Dockerfile` to see the application's Dockerfile", hint: "cat Dockerfile", accept: ["cat Dockerfile"], output: "FROM python:3.11-slim\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install -r requirements.txt\nCOPY . .\nEXPOSE 8000\nCMD [\"uvicorn\", \"main:app\", \"--host\", \"0.0.0.0\"]", note: "Read top to bottom: start from Python 3.11, set up /app, install dependencies, copy code, expose port 8000, run the server. Each instruction creates a layer — Docker caches layers so rebuilds are fast if only your code changed." },
      { learn: "`docker build` reads the Dockerfile and creates an image. `-t` tags it with a name and version. The `.` at the end is the build context — it tells Docker where to find the Dockerfile and files to copy.", task: "Run `docker build -t mymodel:v3 .` to build a new image", hint: "docker build -t mymodel:v3 .", accept: ["docker build -t mymodel:v3 ."], output: "Step 1/7 : FROM python:3.11-slim\nStep 2/7 : WORKDIR /app\n...\nStep 7/7 : CMD [\"uvicorn\"...]\nSuccessfully tagged mymodel:v3", note: "Each step is a Dockerfile instruction. Docker caches steps that haven't changed, so rebuilds after code-only changes are fast. The image is now stored locally — `docker images` lists it." },
      { learn: "`docker run` creates and starts a container from an image. At its simplest, you just pass the image name and it runs.", task: "Run `docker run mymodel:v3` to start a container from the image", hint: "docker run mymodel:v3", accept: ["docker run mymodel:v3"], output: "INFO:  Loading model...\nINFO:  Model loaded. Memory: 14.2GB\nINFO:  Server started on 0.0.0.0:8000", note: "The container is running, but it's attached to your terminal (you can see its output) and the port isn't accessible from outside. Let's fix that with flags." },
      { learn: "In practice, you almost always add flags to `docker run`. `-d` runs the container in the background (detached) so it doesn't block your terminal. `-p 8000:8000` maps a port on your machine to a port inside the container — without this, nothing outside the container can reach it. `--name` gives it a readable name instead of a random ID.", task: "Run `docker run -d -p 8000:8000 --name api mymodel:v3` to run it properly", hint: "docker run -d -p 8000:8000 --name api mymodel:v3", accept: ["docker run -d -p 8000:8000 --name api mymodel:v3"], output: "a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8", note: "The long string is the container ID. Now it's running in the background, port 8000 is accessible, and you can refer to it as `api`. `-p host:container` is the port mapping format — you could map to a different host port like `-p 9000:8000`." },
      { learn: "`docker ps` shows all running containers — their IDs, images, status, ports, and names. This is your go-to command for checking what's running. Add `-a` to also see stopped containers.", task: "Run `docker ps` to see what containers are running", hint: "docker ps", accept: ["docker ps"], output: "CONTAINER ID  IMAGE        STATUS     PORTS                  NAMES\na1b2c3d4e5f6  mymodel:v3   Up 1 min   0.0.0.0:8000->8000/tcp api\nb2c3d4e5f6a1  redis:7      Up 2 days  6379/tcp               redis\nc3d4e5f6a1b2  postgres:15  Up 2 days  5432/tcp               db", note: "Three containers running. The PORTS column shows port mappings. `docker ps -a` includes stopped containers too — useful for finding crashed ones." },
      { learn: "`docker inspect` shows the full configuration and state of a container in JSON format. It reveals everything: IP address, port mappings, environment variables, volume mounts, restart policy, and health status. It's essential for debugging when something isn't working — for example, checking if an environment variable was set correctly or which network the container is on.", task: "Run `docker inspect api` to see the full container details", hint: "docker inspect api", accept: ["docker inspect api"], output: "[\n  {\n    \"Id\": \"a1b2c3d4e5f6...\",\n    \"State\": { \"Status\": \"running\", \"Pid\": 4521 },\n    \"NetworkSettings\": {\n      \"IPAddress\": \"172.17.0.2\",\n      \"Ports\": { \"8000/tcp\": [{ \"HostPort\": \"8000\" }] }\n    },\n    \"Config\": {\n      \"Image\": \"mymodel:v3\",\n      \"Env\": [\"MODEL_PATH=/app/models/v3\"]\n    },\n    \"Mounts\": []\n  }\n]", note: "The output is JSON, so you can pipe it to `jq` for specific fields: `docker inspect api | jq '.[0].NetworkSettings.IPAddress'` returns just the IP. Useful for debugging network issues, checking env vars, or verifying mounts." },
      { learn: "`docker logs -f` follows a container's output in real time, just like `tail -f` for files. This is how you debug what's happening inside a container. `--tail N` shows the last N lines before following.", task: "Run `docker logs -f api` to follow the container's logs", hint: "docker logs -f api", accept: ["docker logs -f api", "docker logs api"], output: "10:42:01 INFO:  Request 245ms\n10:42:03 INFO:  Request 312ms\n10:42:05 ERROR: CUDA out of memory\n10:42:06 INFO:  Falling back to CPU", note: "`docker logs --tail 50 -f api` starts from the last 50 lines then follows live. `Ctrl+C` to stop following." },
      { learn: "`docker exec -it` opens a shell inside a running container so you can poke around and debug. `-i` keeps input open, `-t` allocates a terminal. It's like SSH-ing into a container.", task: "Run `docker exec -it api bash` to get a shell inside the container", hint: "docker exec -it api bash", accept: ["docker exec -it api bash"], output: "root@a1b2c3d4e5f6:/app#", note: "You're inside the container now. Run any command: `ls`, `cat`, `pip list`, `env`. Type `exit` to leave — the container keeps running." },
      { learn: "`docker stop` gracefully stops a running container (sends SIGTERM, waits, then SIGKILL). `docker start` starts a stopped container. `docker restart` does stop then start — useful when you need to pick up new config or clear a stuck process.", task: "Run `docker restart api` to restart the API container", hint: "docker restart api", accept: ["docker restart api"], output: "api", note: "`docker stop api` stops it. `docker start api` starts it again. `docker restart` is the quick combo. `docker rm api` removes a stopped container entirely." },
      { learn: "`docker-compose.yml` (or `compose.yml`) defines a multi-container application. Instead of running each container manually with `docker run`, you describe all your services, their images, ports, volumes, and dependencies in one YAML file. This is how real applications are defined — an API, database, and cache all in one file.", task: "Run `cat docker-compose.yml` to see the multi-container setup", hint: "cat docker-compose.yml", accept: ["cat docker-compose.yml", "cat compose.yml"], output: "services:\n  api:\n    build: .\n    ports:\n      - \"8000:8000\"\n    volumes:\n      - ./data:/app/data\n    depends_on:\n      - db\n      - redis\n  db:\n    image: postgres:15\n    volumes:\n      - db_data:/var/lib/postgresql/data\n  redis:\n    image: redis:7\n\nvolumes:\n  db_data:", note: "Three services defined: `api` (built from our Dockerfile), `db` (Postgres), `redis` (cache). `depends_on` ensures db and redis start before the API. `volumes` persist data so it survives container restarts." },
      { learn: "`docker compose up -d` reads the compose file and starts all services. `-d` runs them in the background. If images don't exist yet, it builds them. If containers already exist, it recreates only the ones that changed.", task: "Run `docker compose up -d` to start all services", hint: "docker compose up -d", accept: ["docker compose up -d", "docker-compose up -d"], output: "Creating network project_default\nCreating db    ... done\nCreating redis ... done\nCreating api   ... done", note: "All three services running with one command. `docker compose logs -f` follows all logs combined. `docker compose ps` shows status of just the compose services." },
      { learn: "Volumes let containers store data that persists even when the container is removed. Without a volume, all data inside a container is lost when it's deleted. In the compose file, `db_data:/var/lib/postgresql/data` means the database files are stored in a named volume called `db_data` — so you can `docker compose down` and `up` again without losing your data.", task: "Run `docker volume ls` to list all volumes", hint: "docker volume ls", accept: ["docker volume ls"], output: "DRIVER    VOLUME NAME\nlocal     project_db_data", note: "The `db_data` volume holds your Postgres data. `docker volume inspect project_db_data` shows where it's stored on disk. Be careful with `docker compose down -v` — the `-v` flag deletes volumes and all their data." },
      { learn: "`docker compose down` stops and removes all containers and networks defined in the compose file. The data in volumes is preserved unless you add `-v`.", task: "Run `docker compose down` to stop all services", hint: "docker compose down", accept: ["docker compose down", "docker-compose down"], output: "Stopping api   ... done\nStopping redis ... done\nStopping db    ... done\nRemoving containers ... done\nRemoving network project_default", note: "`docker compose down` is safe — volumes keep your data. `docker compose down -v` also removes volumes (destructive). `docker compose up -d` brings everything back." },
      { learn: "`docker images` lists all images on your machine. Images pile up over time — especially large ones with ML frameworks. `docker system prune -a` removes everything unused (stopped containers, dangling images, unused networks).", task: "Run `docker images` to list all images and their sizes", hint: "docker images", accept: ["docker images", "docker image ls"], output: "REPOSITORY    TAG     SIZE\nmymodel       v3      8.2GB\nmymodel       v2      7.8GB\npython        3.11    920MB\nredis         7       130MB\npostgres      15      380MB", note: "17GB+ of images. Old versions like v2 waste space. `docker rmi mymodel:v2` removes a specific image. `docker system prune -a` removes all unused images, containers, and networks — run it when disk gets tight." },
      { learn: "`docker stats` shows live CPU, memory, and network usage per container — like `htop` for Docker. Watch for memory approaching the limit, which means an out-of-memory kill is about to happen.", task: "Run `docker stats` to see live resource usage", hint: "docker stats", accept: ["docker stats"], output: "CONTAINER  CPU%   MEM / LIMIT          NET I/O\napi       34.2%  13.8GiB / 15.0GiB   1.2GB / 890MB\nredis      0.3%  42MiB / 15.0GiB     120MB / 95MB\ndb         1.1%  256MiB / 15.0GiB    340MB / 290MB", note: "The API is at 13.8/15GB memory — dangerously close to the limit. `Ctrl+C` to exit. Use this to monitor production containers and catch resource issues before they crash." },
    ],
  },
  {
    id: "nginx", title: "Nginx", icon: "⚡", color: "#22d3ee", difficulty: "advanced",
    desc: "Web server, reverse proxy, routing, SSL, and debugging",
    badge: "Traffic Controller",
    teach: [
      "Nginx (pronounced 'engine-x') is technically a web server, but nowadays it's used mainly as a reverse proxy. That means it sits between the internet and your application: requests from users hit nginx first, and nginx forwards them to your app (usually running in a Docker container on an internal port like 8000). Users never talk to your app directly.",
      "Why bother? Nginx handles HTTPS encryption, spreads traffic across multiple app instances (load balancing), serves static files (images, CSS, JS) without bothering your app, and adds security like rate limiting. A typical setup: Docker containers on internal ports, nginx in front as the single public entry point. This is standard for most modern deployments.",
      "When things go wrong: `502 Bad Gateway` means nginx can't reach your backend (it probably crashed). `504 Gateway Timeout` means the backend is too slow. The nginx error log tells you exactly what happened.",
    ],
    steps: [
      { learn: "`systemctl` (short for 'system control') is the command for managing services on Linux. A service is any program that runs continuously in the background — like a web server, database, or Docker itself. These are managed by `systemd`, the system and service manager built into most Linux distributions. Think of systemd as the supervisor that starts services when your server boots, restarts them if they crash, and tracks their status. `systemctl` is how you talk to systemd. The most common commands: `systemctl status` (check if running), `systemctl start` (start it), `systemctl stop` (stop it), `systemctl restart` (stop then start), `systemctl reload` (re-read config without stopping).", task: "Run `systemctl status nginx` to check if nginx is running", hint: "systemctl status nginx", accept: ["systemctl status nginx"], output: "● nginx.service - A high performance web server\n   Active: active (running) since Mon 2026-03-17\n   Main PID: 1234 (nginx)\n   Memory: 8.2M", note: "Active and running since Monday. `systemctl` works for any service: `systemctl status postgres`, `systemctl status docker`, etc. If it says 'inactive (dead)', the service has stopped. Note: `systemctl` is Linux only (systemd). macOS uses `brew services` (e.g. `brew services list`) or `launchctl`. WSL may need systemd enabled separately." },
      { learn: "The nginx config file defines how traffic is routed. The main config is at `/etc/nginx/nginx.conf`, and individual site configs live in `/etc/nginx/sites-enabled/`. Each `server` block handles a domain. Inside it, `location` blocks match URL paths and decide what to do with the request — usually `proxy_pass` to forward it to your application.", task: "Run `cat /etc/nginx/sites-enabled/default` to read the site config", hint: "cat /etc/nginx/sites-enabled/default", accept: ["cat /etc/nginx/sites-enabled/default", "less /etc/nginx/sites-enabled/default"], output: "server {\n    listen 80;\n    server_name myapp.com;\n\n    location / {\n        proxy_pass http://localhost:3000;\n    }\n    location /api/ {\n        proxy_pass http://localhost:8000/;\n    }\n    location /static/ {\n        root /var/www/myapp;\n    }\n}", note: "Three routes: `/` sends to the frontend on port 3000, `/api/` forwards to the backend on port 8000, `/static/` serves files directly from disk (no app needed). The `listen 80` means this handles HTTP traffic. For HTTPS you'd add `listen 443 ssl` with certificate paths." },
      { learn: "`nginx -t` tests your config for syntax errors without applying it. This is critical — a typo in the config will take your entire site down when you reload. Always test before applying. The workflow is: edit config → `nginx -t` → reload.", task: "Run `sudo nginx -t` to test the config for syntax errors", hint: "sudo nginx -t", accept: ["sudo nginx -t", "nginx -t"], output: "nginx: configuration file syntax is ok\nnginx: configuration test is successful", note: "Config is valid. If there's an error, it tells you the exact file and line number. Never skip this step — one missing semicolon can cause a full outage." },
      { learn: "`systemctl reload` tells nginx to re-read its config files without stopping. Active connections stay open — zero downtime for users. This is how you apply config changes in production. Only use `restart` if `reload` fails.", task: "Run `sudo systemctl reload nginx` to apply changes gracefully", hint: "sudo systemctl reload nginx", accept: ["sudo systemctl reload nginx", "sudo nginx -s reload"], output: "", note: "Config applied with zero downtime. The safe workflow: `vim /etc/nginx/sites-enabled/default` → `sudo nginx -t` → `sudo systemctl reload nginx`. Never reload without testing first." },
      { learn: "The nginx error log at `/var/log/nginx/error.log` is your first stop when things break. It tells you exactly why requests fail: 'connection refused' means your backend is down, 'upstream timed out' means it's too slow, 'no resolver defined' means DNS isn't configured.", task: "Run `tail -f /var/log/nginx/error.log` to watch the error log live", hint: "tail -f /var/log/nginx/error.log", accept: ["tail -f /var/log/nginx/error.log", "tail -n 50 /var/log/nginx/error.log"], output: "[error] connect() failed (111: Connection refused)\n  upstream: \"http://127.0.0.1:8000/predict\"\n[error] upstream timed out (110: Connection timed out)\n  upstream: \"http://127.0.0.1:8000/train\"", note: "'Connection refused' (111) = backend is not running, restart it. 'Timed out' (110) = backend is too slow, investigate why. This log is always the answer to 'why am I getting 502s?'" },
      { learn: "The access log records every request that hits nginx: the client's IP, what URL they requested, the HTTP status code, and the response size. This is your traffic dashboard — you can see what's being accessed, spot error spikes, and identify suspicious patterns.", task: "Run `tail -f /var/log/nginx/access.log` to watch incoming requests", hint: "tail -f /var/log/nginx/access.log", accept: ["tail -f /var/log/nginx/access.log", "tail -n 50 /var/log/nginx/access.log"], output: "203.45.67.89 - - [18/Mar/2026:10:42:01] \"GET /api/predict HTTP/1.1\" 200 245\n203.45.67.89 - - [18/Mar/2026:10:42:03] \"GET /api/predict HTTP/1.1\" 502 0\n10.0.1.15    - - [18/Mar/2026:10:42:05] \"GET / HTTP/1.1\" 200 1823", note: "Status codes: `200` = success, `301/302` = redirect, `404` = not found, `502` = backend down, `504` = backend timeout. The second request got a 502 — the backend was down at that moment. You can pipe this to `grep` or `awk` for analysis: `grep ' 502 ' access.log | wc -l` counts how many 502s occurred." },
      { learn: "Use `curl` to test that nginx is routing requests correctly. `-I` fetches only the response headers (not the body), which is faster and shows you the status code, server info, and custom headers.", task: "Run `curl -I localhost/api/health` to verify nginx routes to the backend", hint: "curl -I localhost/api/health", accept: ["curl -I localhost/api/health", "curl localhost/api/health", "curl -I localhost:8000/health", "curl localhost:8000/health"], output: "HTTP/1.1 200 OK\nServer: nginx/1.24.0\nContent-Type: application/json\nX-Response-Time: 12ms", note: "`200 OK` through nginx means the full path works: internet → nginx (port 80) → backend (port 8000). If you get a 502 here, check `tail /var/log/nginx/error.log` to find out why." },
      { learn: "`systemctl restart` fully stops and starts a service. Unlike `reload`, it drops all active connections — there's a brief moment of downtime. Use it when `reload` fails or after major changes (like upgrading nginx itself).", task: "Run `sudo systemctl restart nginx` for a full restart", hint: "sudo systemctl restart nginx", accept: ["sudo systemctl restart nginx"], output: "", note: "Brief downtime while it restarts. Prefer `reload` for config changes (zero downtime). Use `restart` after package upgrades or when nginx is in a broken state. `sudo systemctl stop nginx` stops it entirely, `sudo systemctl start nginx` starts it." },
      { learn: "`journalctl` (short for 'journal control') is the log viewer for systemd. Remember that systemd manages all your services — it also captures everything they print to stdout/stderr and stores it in a central journal. While nginx has its own log files (access.log, error.log), `journalctl` captures the service-level events: when it started, stopped, crashed, or failed to reload. `-u nginx` filters to just nginx logs, `-n 20` shows the last 20 entries. This is where you'll find errors that don't appear in the application's own log files.", task: "Run `journalctl -u nginx --no-pager -n 20` to check nginx service logs", hint: "journalctl -u nginx --no-pager -n 20", accept: ["journalctl -u nginx --no-pager -n 20", "journalctl -u nginx -n 20", "journalctl -u nginx"], output: "Mar 18 10:42:00 prod nginx[1234]: nginx: configuration test is successful\nMar 18 10:42:00 prod systemd[1]: Reloaded nginx.service\nMar 18 10:42:01 prod nginx[1234]: [notice] signal process started", note: "`journalctl` works for any service: `-u postgres`, `-u docker`, etc. Add `-f` to follow live. It's especially useful for debugging startup failures — if a service won't start, `journalctl -u servicename` tells you why. Note: `journalctl` is Linux only (systemd). On macOS, use `log show` or check `/var/log/` for service logs." },
    ],
  },
  {
    id: "boss", title: "Production Incident", icon: "🚨", color: "#ef4444", difficulty: "capstone",
    desc: "11pm. API is down. Everything you learned, combined.",
    badge: "Incident Commander",
    teach: [
      "It's 11pm. Your phone buzzes — the monitoring dashboard shows 502 errors spiking on the inference API. Users are getting failures. This is a production incident, and you're on call.",
      "This chapter is the final boss: a realistic incident response scenario that combines everything you've learned. You'll SSH into the server, start a tmux session, assess system resources, dig through logs, identify the root cause, and fix it.",
      "The incident response pattern: connect → `tmux` (so you don't lose your session) → assess (CPU, memory, disk, processes) → diagnose (logs, error messages) → fix (restart services, free resources) → verify (confirm the fix worked).",
      "Every command you've practiced — `ssh`, `tmux`, `ps`, `grep`, `tail`, `docker`, `nginx`, `kill` — comes together here. This is what real server debugging looks like.",
    ],
    steps: [
      { learn: "11pm alert: 502 errors spiking on the inference API. Users are getting errors. First step: get on the server. SSH config aliases get you connected in two words — in a real incident, every second counts.", task: "Run `ssh prod` to connect to the production server", hint: "ssh prod", accept: ["ssh prod"], output: "deploy@prod:~$", note: "Two words. You're in." },
      { learn: "Always start `tmux` during incidents. If your WiFi drops mid-fix, you lose your terminal — but with `tmux`, just reconnect and `tmux attach`. Never debug production without it.", task: "Run `tmux new -s incident` to protect your session", hint: "tmux new -s incident", accept: ["tmux new -s incident"], output: "[incident] 0:bash*", note: "Protected. Everything from here is safe." },
      { learn: "Quick health check: how much memory is in use? If the inference model needs 14GB and only 2GB is in use, something that was running has stopped. This is our first clue.", task: "Run `free -h` to check how much RAM is in use", hint: "free -h", accept: ["free -h"], output: "              total    used    free   available\nMem:           15Gi   2.1Gi    11Gi      12Gi", note: "Only 2GB used on a box that should be running a 14GB model. Something's dead." },
      { learn: "`docker ps` shows running containers, but `-a` reveals stopped ones too. Exit code `137` means the kernel's OOM killer terminated the process — it ran out of memory. This is a very common cause of container crashes.", task: "Run `docker ps -a` to find stopped/crashed containers", hint: "docker ps -a", accept: ["docker ps -a"], output: "CONTAINER ID  IMAGE        STATUS                      NAMES\na1b2c3d4e5f6  redis:7      Up 2 days                   redis\nf6e5d4c3b2a1  mymodel:v2   Exited (137) 23 min ago     inference_api", note: "Exit `137` = OOM kill. The kernel terminated it 23 minutes ago." },
      { learn: "Containers keep their logs even after crashing. `--tail 20` shows the last 20 lines before death. We're looking for the sequence of events: what was the container doing right before it died?", task: "Run `docker logs inference_api --tail 20` to read the crash logs", hint: "docker logs inference_api --tail 20", accept: ["docker logs inference_api --tail 20", "docker logs inference_api"], output: "22:17:42 INFO:  Model loaded. Memory: 14.2GB\n22:18:03 WARN:  Memory at 95%\n22:18:05 INFO:  Processing batch of 64\n22:18:06 FATAL: Killed", note: "14.2GB model on 15GB box. Heavy load pushed it over. Tomorrow: reduce batch size, add limits. Now: restart." },
      { learn: "Before restarting anything, check disk space. If the disk is full, the container will just crash again immediately. A full disk can also prevent logging, causing silent failures.", task: "Run `df -h` to check disk space before restarting", hint: "df -h", accept: ["df -h"], output: "Filesystem      Size  Used Avail Use% Mounted on\n/dev/nvme0n1p1  200G   42G  158G  21% /\n/dev/nvme1n1    500G  312G  188G  63% /data", note: "Disk is fine — 158GB free on root, 188GB on `/data`. Not a disk issue." },
      { learn: "`docker restart` stops and starts a container. Since we've verified disk is fine, the OOM was likely a spike — restarting should work. If there's a systemic issue (disk full, config broken), it'll crash again.", task: "Run `docker restart inference_api` to bring it back up", hint: "docker restart inference_api", accept: ["docker restart inference_api"], output: "inference_api", note: "Starting up. Give it a few seconds to load the model." },
      { learn: "Follow the boot sequence in real time. We need to see: model loading (takes ~13 seconds for a 14GB model), server binding to port 8000, and health check passing. If any step fails, we'll see the error here.", task: "Run `docker logs -f inference_api` to watch it boot", hint: "docker logs -f inference_api --tail 5", accept: ["docker logs -f inference_api --tail 5", "docker logs -f inference_api"], output: "22:23:01 INFO:  Loading model...\n22:23:14 INFO:  Model loaded. Memory: 14.2GB\n22:23:14 INFO:  Server started on 0.0.0.0:8000\n22:23:15 INFO:  Health check passed", note: "Model loaded, server started, health check passed. Looking good." },
      { learn: "Don't trust logs alone — verify the API actually responds to requests. `curl` is ground truth: if this returns `200`, the service is genuinely back. If it times out or errors, we have a different problem.", task: "Run `curl localhost:8000/health` to verify the API responds", hint: "curl localhost:8000/health", accept: ["curl localhost:8000/health", "curl http://localhost:8000/health"], output: "{\"status\":\"healthy\",\"model\":\"v2\",\"uptime\":\"14s\"}", note: "API is live. 502s stop. Incident resolved. Write the postmortem tomorrow." },
      { learn: "The API container is up, but users don't connect to it directly — they go through nginx (the reverse proxy) first. We tested the container directly on port 8000 in the last step, but now we need to test the full path that real users take: internet → nginx (port 80) → container (port 8000). The `-I` flag tells curl to only fetch the response headers (not the body), which is a quick way to check the HTTP status code and confirm which server handled the request.", task: "Run `curl -I localhost/api/health` to verify nginx routes correctly", hint: "curl -I localhost/api/health", accept: ["curl -I localhost/api/health", "curl localhost/api/health"], output: "HTTP/1.1 200 OK\nServer: nginx/1.24.0\nX-Upstream: 127.0.0.1:8000", note: "`200` through nginx. The `Server: nginx` header confirms nginx handled the request and forwarded it to the container. The full path is working. Crisis over. Go to sleep. You've completed the capstone — now practice these commands on real servers." },
    ],
  },
  {
    id: "ch-deploy", title: "Ship It", icon: "🚢", color: "#ef4444", difficulty: "challenge",
    mode: "challenge",
    desc: "Deploy a web application on a fresh server",
    badge: "Deployer",
    objective: "You've been given a bare Ubuntu server and a git repo. Get the web app live and responding to requests.",
    teach: [
      "This is a challenge — there's no step-by-step guide. You have a set of objectives to complete in any order. Figure out what commands to run. If you're stuck, check the nudge (Tab) or type `solution` to reveal the exact command.",
      "Scenario: A fresh Ubuntu server with nginx installed but not configured. The app repo is at https://github.com/acme/webapp.git. Your job: clone it, install deps, build it, configure nginx, and verify it's live.",
    ],
    steps: [
      { task: "Clone the application repository", nudge: "You need to get the code onto the server first. What command downloads a git repo?", hint: "git clone https://github.com/acme/webapp.git", accept: ["git clone https://github.com/acme/webapp.git"], output: "Cloning into 'webapp'...\nremote: Enumerating objects: 847, done.\nremote: Total 847, delta 312\nReceiving objects: 100% (847/847), 2.1 MiB | 12.3 MiB/s, done.", note: "Repo cloned. The source code is now in ./webapp/" },
      { task: "Navigate into the project directory", nudge: "You need to be inside the project to run build commands.", hint: "cd webapp", accept: ["cd webapp"], output: "", note: "You're now in the project root." },
      { task: "Install the project dependencies", nudge: "Most Node.js projects list their dependencies in package.json.", hint: "npm install", accept: ["npm install", "npm i"], output: "added 847 packages in 12s\n\n142 packages are looking for funding\n  run `npm fund` for details", note: "Dependencies installed from package.json." },
      { task: "Build the project for production", nudge: "Check package.json scripts — there's usually a build command.", hint: "npm run build", accept: ["npm run build"], output: "vite v5.2.0 building for production...\n✓ 284 modules transformed.\ndist/index.html          0.54 kB\ndist/assets/index.js   142.30 kB\n✓ built in 3.2s", note: "Production build output is in dist/. This is what nginx will serve." },
      { task: "Check the nginx site configuration", nudge: "nginx site configs live in /etc/nginx/sites-available/. Try `cat` to read the default config file.", hint: "cat /etc/nginx/sites-available/default", accept: ["cat /etc/nginx/sites-available/default", "cat /etc/nginx/sites-enabled/default", "cat /etc/nginx/nginx.conf"], output: "server {\n    listen 80;\n    server_name _;\n    root /home/ubuntu/webapp/dist;\n    index index.html;\n\n    location / {\n        try_files $uri $uri/ /index.html;\n    }\n}", note: "The config already points to /home/ubuntu/webapp/dist — which is where your build output went. Convenient." },
      { task: "Test the nginx configuration for errors", nudge: "nginx has a built-in config test flag: `nginx -t`. You'll need sudo.", hint: "sudo nginx -t", accept: ["sudo nginx -t", "nginx -t"], output: "nginx: the configuration file /etc/nginx/nginx.conf syntax is ok\nnginx: configuration file /etc/nginx/nginx.conf test is successful", note: "Config is valid. No syntax errors." },
      { task: "Reload nginx to apply the new config", nudge: "Use `systemctl` to reload or restart the nginx service.", hint: "sudo systemctl reload nginx", accept: ["sudo systemctl reload nginx", "sudo systemctl restart nginx", "sudo nginx -s reload", "systemctl reload nginx", "systemctl restart nginx"], output: "", note: "nginx reloaded with the new config." },
      { task: "Verify the app is live and responding", nudge: "Make an HTTP request to localhost to check if the server responds.", hint: "curl localhost", accept: ["curl localhost", "curl http://localhost", "curl http://localhost/"], output: "<!DOCTYPE html>\n<html>\n<head><title>Acme Webapp</title></head>\n<body><div id=\"root\"></div>\n<script src=\"/assets/index.js\"></script>\n</body></html>", note: "The app is live! HTML is being served from dist/. Ship it! 🚢" },
    ],
  },
  {
    id: "ch-debug", title: "Find the Bug", icon: "🔍", color: "#ef4444", difficulty: "challenge",
    mode: "challenge",
    desc: "Diagnose why the API is returning 500 errors",
    badge: "Debugger",
    objective: "Users are reporting intermittent 500 errors from the API. The app runs in a Docker container behind nginx. Find the root cause and fix it.",
    teach: [
      "This is a challenge — no step-by-step guide. Complete the objectives in any order. Use Tab for nudges or type `solution` for the answer.",
      "Scenario: The API (running as a Docker container called 'api') is returning 500 errors. nginx proxies traffic to it. Something is wrong — could be the app, the container, disk space, or resource exhaustion. Investigate and fix it.",
    ],
    steps: [
      { task: "Check the nginx error logs for clues", nudge: "Web server error logs are your first stop. Where does nginx keep them?", hint: "tail /var/log/nginx/error.log", accept: ["tail /var/log/nginx/error.log", "cat /var/log/nginx/error.log", "tail -n 20 /var/log/nginx/error.log"], output: "2024/03/15 14:23:01 [error] connect() failed (111: Connection refused) while connecting to upstream\n2024/03/15 14:23:02 [error] connect() failed (111: Connection refused) while connecting to upstream\n2024/03/15 14:23:05 [error] connect() failed (111: Connection refused) while connecting to upstream", note: "Connection refused to upstream — nginx can't reach the API container. The container might be down." },
      { task: "Check if the API container is running", nudge: "How do you see the status of Docker containers, including stopped ones?", hint: "docker ps -a", accept: ["docker ps -a", "docker ps"], output: "CONTAINER ID  IMAGE       STATUS                      NAMES\na1b2c3d4e5f6  redis:7     Up 3 days                   redis\nf6e5d4c3b2a1  api:v3      Exited (137) 5 min ago      api", note: "The API container exited with code 137 — that's an OOM kill. It ran out of memory." },
      { task: "Check the API container's logs for what happened", nudge: "Docker keeps logs even for stopped containers.", hint: "docker logs api --tail 20", accept: ["docker logs api --tail 20", "docker logs api"], output: "14:22:55 INFO:  Processing request batch (128 items)\n14:22:58 WARN:  Memory usage at 94%\n14:23:00 WARN:  Memory usage at 98%\n14:23:01 FATAL: Killed", note: "The container was processing a large batch and ran out of memory. OOM killer terminated it." },
      { task: "Check system disk space", nudge: "Before restarting, make sure there's enough disk space. A full disk causes cascading failures.", hint: "df -h", accept: ["df -h"], output: "Filesystem      Size  Used Avail Use% Mounted on\n/dev/sda1       100G   23G   77G  23% /\n/dev/sdb1       500G  189G  311G  38% /data", note: "Disk is fine — plenty of space. The issue is memory, not disk." },
      { task: "Check for any runaway processes consuming resources", nudge: "Something might be eating CPU or memory. What shows all running processes?", hint: "ps aux --sort=-%mem | head -10", accept: ["ps aux --sort=-%mem | head -10", "ps aux --sort=-%mem", "ps aux | head -10", "ps aux"], output: "USER       PID %CPU %MEM    VSZ   RSS TTY      STAT COMMAND\nroot      1842 95.2 45.3 984532 462108 ?       R    /usr/bin/stress --vm 4\nroot      1001  0.3  2.1  84532  21108 ?       Ss   /usr/sbin/nginx\nubuntu    1102  0.1  0.8  42312   8432 ?       Ss   /usr/bin/redis-server", note: "There it is! PID 1842 is a `stress` process using 95% CPU and 45% memory. That's not supposed to be there." },
      { task: "Kill the runaway process", nudge: "You know the PID of the rogue process. What command terminates a process by PID?", hint: "kill 1842", accept: ["kill 1842", "kill -9 1842", "sudo kill 1842", "sudo kill -9 1842"], output: "", note: "Rogue process killed. Memory should free up now." },
      { task: "Restart the API container", nudge: "The container crashed. Now that resources are freed, bring it back.", hint: "docker restart api", accept: ["docker restart api"], output: "api", note: "Container restarting..." },
      { task: "Verify the API is responding", nudge: "Make an HTTP request to check if the API health endpoint works.", hint: "curl localhost/api/health", accept: ["curl localhost/api/health", "curl http://localhost/api/health", "curl localhost:8080/health"], output: "{ \"status\": \"ok\", \"uptime\": \"3s\" }", note: "API is back online! Root cause: a rogue `stress` process was eating resources, causing the API container to OOM. 🔍" },
    ],
  },
  {
    id: "ch-lockdown", title: "Lock It Down", icon: "🔒", color: "#ef4444", difficulty: "challenge",
    mode: "challenge",
    desc: "Secure a server with multiple security vulnerabilities",
    badge: "Guardian",
    objective: "A security audit flagged several issues on this server. Find and fix all the vulnerabilities before the auditor comes back.",
    teach: [
      "This is a challenge — no step-by-step guide. Complete the objectives in any order. Use Tab for nudges or type `solution` for the answer.",
      "Scenario: The security team found problems: overly permissive file permissions, a suspicious process, exposed secrets, and world-writable scripts. Lock it all down.",
    ],
    steps: [
      { task: "Fix the SSH private key — it's too permissive", nudge: "SSH keys should only be readable by their owner. What's the right permission?", hint: "chmod 600 ~/.ssh/id_rsa", accept: ["chmod 600 ~/.ssh/id_rsa"], output: "", note: "SSH keys must be 600 (owner read/write only). SSH actually refuses to use keys with looser permissions — it'll say 'Permissions too open' and reject the key." },
      { task: "Fix the exposed secrets file", nudge: "The .env file contains API keys and passwords. It shouldn't be world-readable.", hint: "chmod 600 .env", accept: ["chmod 600 .env"], output: "", note: ".env locked down. In production, consider using a secrets manager (AWS Secrets Manager, Vault) instead of .env files entirely." },
      { task: "Check for suspicious running processes", nudge: "Are there any processes running that shouldn't be? List all processes.", hint: "ps aux", accept: ["ps aux", "ps aux | grep -v grep"], output: "USER       PID %CPU %MEM COMMAND\nroot         1  0.0  0.1 /sbin/init\nroot       234  0.0  0.3 /usr/sbin/sshd\nroot       456  0.0  0.2 /usr/sbin/nginx\nnobody     789  2.1  1.4 /tmp/.hidden/cryptominer\nubuntu    1001  0.0  0.1 bash", note: "PID 789 — a cryptominer running from /tmp as 'nobody'. Classic compromise indicator. This needs to go." },
      { task: "Kill the unauthorized process", nudge: "You found the suspicious process. Terminate it.", hint: "sudo kill -9 789", accept: ["kill 789", "kill -9 789", "sudo kill 789", "sudo kill -9 789"], output: "", note: "Cryptominer killed. In a real incident, you'd also investigate how it got there (check auth logs, cron jobs, open ports)." },
      { task: "Check what ports are open and listening", nudge: "Use `netstat -tlnp` to see which processes are listening on which ports. Look for anything unexpected.", hint: "netstat -tlnp", accept: ["netstat -tlnp", "netstat -tunlp", "ss -tlnp", "ss -tunlp", "lsof -i", "sudo netstat -tlnp"], output: "Proto  Local Address     State   PID/Program\ntcp    0.0.0.0:22        LISTEN  234/sshd\ntcp    0.0.0.0:80        LISTEN  456/nginx\ntcp    0.0.0.0:4444      LISTEN  891/nc", note: "Port 4444 with `nc` (netcat) — that's a reverse shell listener. Definitely not supposed to be there. The cryptominer likely set this up." },
      { task: "Fix the world-writable deploy script", nudge: "A deploy script that anyone can modify is a supply chain risk.", hint: "chmod 755 deploy.sh", accept: ["chmod 755 deploy.sh", "chmod 750 deploy.sh"], output: "", note: "deploy.sh is now 755 (owner: rwx, group: r-x, others: r-x). World-writable (777) scripts are a common attack vector — an attacker modifies the script, and the next deploy runs their code." },
      { task: "Review the authentication logs for break-in attempts", nudge: "Auth logs show SSH login attempts — both successful and failed.", hint: "cat /var/log/auth.log | tail -20", accept: ["cat /var/log/auth.log", "tail /var/log/auth.log", "cat /var/log/auth.log | tail -20", "tail -20 /var/log/auth.log"], output: "Mar 15 08:14:22 server sshd: Failed password for root from 45.33.32.156 port 42312\nMar 15 08:14:23 server sshd: Failed password for root from 45.33.32.156 port 42313\nMar 15 08:14:24 server sshd: Failed password for root from 45.33.32.156 port 42314\nMar 15 09:01:15 server sshd: Accepted publickey for nobody from 103.25.17.89\nMar 15 09:01:16 server sshd: pam_unix(sshd:session): session opened for user nobody", note: "Brute force attempts from 45.33.32.156, then a successful login as 'nobody' from a different IP. That's the compromise point. In production: disable password auth, use fail2ban, restrict SSH to known IPs. Server secured! 🔒" },
    ],
  },
  {
    id: "ai", title: "AI in the Terminal", icon: "🤖", color: "#a78bfa", difficulty: "intermediate",
    desc: "Use AI coding assistants directly from the command line",
    badge: "AI Operator",
    teach: [
      "AI coding assistants aren't just chat windows — the most powerful ones run directly in your terminal. They can read your codebase, edit files, run commands, and work alongside you like a pair programmer that understands your entire project.",
      "Claude Code is an agentic coding tool that lives in your terminal. You give it a task in natural language, and it reads files, writes code, runs tests, and commits changes — all while you watch and approve each step. It requires Node.js (v18+).",
      "This chapter walks through the full setup and common workflows. The terminal skills you've been learning are exactly the foundation you need — Claude Code runs in the same environment, uses the same tools, and makes you faster at everything you already know.",
    ],
    steps: [
      { learn: "Claude Code requires Node.js version 18 or higher. Before installing anything, check if Node.js is already on your system and what version it is. If you see a version below 18, you'll need to upgrade. If you get 'command not found', install it from nodejs.org or with your package manager (`brew install node` on macOS, `sudo apt install nodejs` on Ubuntu).", task: "Run `node --version` to check if Node.js is installed", hint: "node --version", accept: ["node --version", "node -v"], output: "v20.11.0", note: "v20 — that's above v18, so you're good. If you got 'command not found': install from nodejs.org, or `brew install node` (macOS), `sudo apt install nodejs` (Ubuntu)." },
      { learn: "Claude Code is distributed as an npm package. `npm` is the Node.js package manager — it comes with Node.js. The `-g` flag installs it globally, meaning the `claude` command is available from any directory, not just one project.", task: "Install Claude Code globally with `npm install -g @anthropic-ai/claude-code`", hint: "npm install -g @anthropic-ai/claude-code", accept: ["npm install -g @anthropic-ai/claude-code", "npm i -g @anthropic-ai/claude-code", "sudo npm install -g @anthropic-ai/claude-code", "sudo npm i -g @anthropic-ai/claude-code"], output: "added 1 package\n\ninstalled @anthropic-ai/claude-code@1.0.0", note: "Installed. If you get a permissions error, use `sudo` on Linux/macOS or fix npm permissions (see npm docs). Now `claude` is a command." },
      { learn: "Running `claude` with no arguments starts an interactive session. Navigate to your project directory first — Claude reads the files around it to understand context. On first launch, it will prompt you to log in to your Anthropic account — no API key setup needed. It opens an agentic REPL where you type natural language prompts and Claude reads your project, suggests changes, and executes commands with your approval.", task: "Start Claude Code by typing `claude` in your project directory", hint: "claude", accept: ["claude"], output: "╭──────────────────────────────────╮\n│  Claude Code v1.0                │\n│  Type a prompt or /help          │\n╰──────────────────────────────────╯\n\n  >", note: "You're in an interactive session. Claude can see your project files and suggest changes. Type naturally — like talking to a teammate. Type `/help` to see slash commands, `/quit` to exit." },
      { learn: "You can pass a prompt directly as an argument for one-shot tasks — no interactive session needed. Claude reads your codebase for context and responds. This is great for quick questions, and for scripting or piping into other tools.", task: "Ask Claude to explain a file: `claude \"explain src/train.py\"`", hint: "claude \"explain src/train.py\"", accept: ["claude \"explain src/train.py\"", "claude 'explain src/train.py'", "claude explain src/train.py"], output: "src/train.py is a PyTorch training script that:\n  - Loads data from /data/processed/\n  - Defines a transformer model\n  - Trains for 50 epochs with early stopping\n  - Saves checkpoints to /models/", note: "Claude read the file and summarized it. One-shot mode is great for quick answers: `claude \"what does this regex do\"`, `claude \"find where auth is handled\"`." },
      { learn: "Claude Code can run terminal commands on your behalf — with your approval. When it needs to run a shell command, it shows you the exact command first and waits for you to press Y or N. You always see what's about to run before it executes. This is how it runs tests, installs packages, starts servers, etc.", task: "Ask Claude to run tests: `claude \"run the test suite\"`", hint: "claude \"run the test suite\"", accept: ["claude \"run the test suite\"", "claude 'run the test suite'", "claude run the test suite"], output: "I'll run the test suite for you.\n\n  $ python -m pytest tests/ -v\n\n  Allow? [Y/n] Y\n\n  tests/test_model.py::test_forward ✓\n  tests/test_model.py::test_backward ✓\n  2 passed in 1.42s", note: "Claude proposed `pytest`, you approved, it ran. The propose → confirm → execute pattern keeps you in control. Deny with N if the command looks wrong." },
      { learn: "Piping works with Claude too — standard Unix pipes. You can pipe output from any command into Claude for analysis, or pipe Claude's output into other tools. This combines traditional Unix tools with AI: use `grep`, `awk`, `sort` for structured data, and Claude for understanding meaning.", task: "Pipe a log file into Claude: `cat /var/log/app.log | claude \"summarize any errors\"`", hint: "cat /var/log/app.log | claude \"summarize any errors\"", accept: ["cat /var/log/app.log | claude \"summarize any errors\"", "cat /var/log/app.log | claude 'summarize any errors'"], output: "Found 3 errors in the log:\n  1. OOM at 22:18 — model exceeded memory limit\n  2. Connection refused at 22:19 — Redis unreachable\n  3. Timeout at 22:20 — upstream didn't respond in 30s\n\nThe OOM triggered a cascade: model crashed → API down → timeout errors.", note: "Pipe anything text-based into Claude: `git diff | claude \"review this\"`, `docker logs app | claude \"why is it crashing\"`, `cat error.log | claude \"explain\"`." },
    ],
  },
];
